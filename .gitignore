.node-env
.env
.venv
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
node_modules/
.next/
*.tsbuildinfo
dist/
coverage/
logs/
*.db
*.sqlite3
.verify/

# pytest 自定义 basetemp（M9-08 本地验证残留）
.pytest-root*
.pytest-tmp*
services/api/.pytest-*

# M10-04 治理报告/导出（含生产 ID 清单，禁止提交）
artifacts/
temp/

# M13-01 HarmonyOS 构建产物(HAP/cache/intermediates/绝对路径)禁止入库
apps/harmony/entry/build/

# HarmonyOS hvigor 构建缓存(仓库根 .hvigor/)禁止入库
/.hvigor/

# M13-10 AGC/HarmonyOS 签名材料防护:发布证书/Profile/密钥库等材料禁止入库
# (仓库内当前无任何此类文件;材料只能放仓库外,经 AIOS_HARMONY_*_PATH 环境变量引用,
#  并用 tools/harmony_release/preflight.py 做前置校验)
*.p12
*.p7b
*.cer
*.csr
*.jks

# M14-06 生产恢复编排 env 文件（含部署 secret，仅本机存在；模板见
# infra/env.production-recovery.example，恢复编排 pin check 强制其存在与一致）
infra/env.production-recovery

# M14-141 production drift watch 告警分发 webhook secret JSON（操作者按
# M14-135 secret 文件语义创建于固定路径；scheduler/wrapper 仅存在性检查，
# 绝不读取内容——内容校验全部由 M14-135 承担）
infra/env.production-drift-watch-alert-secret.json
