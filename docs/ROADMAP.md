# 实施路线

## M0 工程基线

- [x] monorepo 与 Next.js/FastAPI 分层
- [x] Grok 视觉资产迁移
- [x] 服务端权威考试 API 草案
- [x] Docker Compose 基础设施
- [x] Git 初始化
- [ ] 远程仓库
- [ ] CI
- [ ] PostgreSQL repository

## M1 内容与来源

- [ ] Source Registry
- [ ] License State Machine
- [ ] 文件上传与 hash 去重
- [ ] Docling/MinerU/Marker/olmOCR adapter
- [ ] Evidence 与页码定位

## M2 考试与评分

- [x] 内存版 ExamSession
- [x] 答案事件序列
- [x] 幂等提交
- [ ] PostgreSQL FSM 持久化
- [ ] Redis timeout worker
- [ ] 数学/主观题 grader

## M3 学习模型

- [ ] LearningEvent 标准化
- [ ] Concept DAG
- [ ] StudentConceptState 重放
- [ ] FSRS-like scheduler

## M4 语音

- [x] 浏览器本地语音过渡实现
- [ ] LiveKit server/token
- [ ] FunASR/CosyVoice 本地 adapter
- [ ] 在线 provider fallback
- [ ] VoiceSession FSM 与打断恢复

## M5 检索

- [ ] 多源搜索
- [ ] 去重排序
- [ ] 受控抓取
- [ ] 课程/试卷导入审核

## M12 原生 Android

- [x] M12-01 App Shell + API/Auth 基础（Compose M3 壳、五屏、登录/会话、可配置 API 地址、Keystore 加密 token）
- [x] 考试/学习业务接入（M12-02 已随 PR #45 合并 main——学习试卷列表、考场状态机、审阅报告，PR CI 与 merge 后 main CI 全绿实证；仍未跑模拟器/真机）
- [x] 语音能力接入（M12-03 第一切片已随 PR #47 合并 main，merge commit `2bbeb072`——服务端权威语音陪练：创建/恢复会话、读题播报链、命令/意图作答、打断/暂停/跳过/结束、录音转写代理、全卷报告、trace 上报；PR CI run `34049713374` 与 merge 后 main CI run `34049946924` 四项全绿实证；仍未跑模拟器/真机、未接真实 provider）
- [x] 搜索接入（M12-04 第一切片已随 PR #49 合并 main，merge commit `64e26caeb83645e49a8f2db87dbcbfd97ea524e0`，PR CI run `34073874737` 与 merge 后 main CI run `34074112566` 四项全绿——服务端权威搜索 providers/plan/search/record、318 JVM 单测、lint 0 error；Android 16 emulator-5554 loopback mock 冒烟已通过；仍未验真机、真实 provider、非 https 禁用态模拟器交互与生产可用边界）
- [x] 治理与发布链路接入（只读第一切片）（M12-05 已随 PR #51 合并 main，merge commit `8c6fe6b6354c504e8e61beb078bfe2675b7f558a`——Android 只读消费 /version、/ops-snapshot、/audit 三端点、347 JVM 单测、lint 0 error；PR CI run `34085123408` 与 merge 后 main CI run `34085414607` 四项全绿（API/Android/Docker/Web）；Android 16 emulator-5554 loopback mock 冒烟通过（证据 `docs/evidence/m12-05-android-governance/`）；仍未验真机、真实 provider、生产后端/生产 DB、治理写操作与生产可用）
- [x] loopback 冒烟 harness（M12-06 已随 PR #53 合并 main，merge commit `4cbdfd2512891b36443c2caa5d967769567fcfac`——tools/android_smoke + tests/android_smoke（18 files / +4566），Python 单测 215 passed / 1 skipped；真实 emulator-5554 冒烟 15 stage passed、mock expected 18 / unexpected 0 / total 18、logcat 本包 FATAL/ANR/crash=0（AndroidRuntime 标签行 145 为 uiautomator 工具进程生命周期日志的仅诊断计数，非崩溃）；首提交引入的根 tests/__init__.py 由 `e505716` 删除以避免与 services/api 测试包冲突；PR CI run `34132047633` 与 merge 后 main CI run `34132434939` 四项全绿；仍未验真机、真实 provider 与生产链路，证据 .verify/m12-06-android-smoke-r8 为 gitignored 本地目录不入库）
- [ ] 治理与发布链路接入第二切片（待运维授权评估：真机验证、真实 provider 冒烟、治理写链路/生产接入——治理写操作与生产链路不随只读第一切片视为完成）

## M13 HarmonyOS

- [x] M13-01 只读壳 + API/Auth/设置（已随 PR #54 合并 main，merge commit `cedf862e4b940d7c637378eea40be2247ba4249d`——ArkTS 五 Tab 只读壳（com.ailearningos.app）、AiosApi 只读 GET 六端点（METHOD_GET 硬编码）、UrlPolicy base URL 白名单、Settings 仅存 base URL（preferences 唯一键 api_base_url，零 token/password/key）、仅 ohos.permission.INTERNET 权限；本地模拟器（hdc -t 127.0.0.1:5557）clean assembleHap / 未签名 HAP 安装 / 启动 / 五 Tab 真实点击 / 六张 1320x2856 非空截图验收通过（证据 `docs/evidence/m13-01-harmony-shell-api-auth/`，随 PR 入库）；PR CI run `34132471980` 与 merge 后 main CI run `34132893106` 四项全绿（API/Android/Docker/Web——CI 无 HarmonyOS job，HarmonyOS 构建验收为本地口径）；未验真机、未接真实 provider/生产 DB/生产后端/治理写链路，production_ready=false 不变）
- [x] M13-02 Home mock 数据与降级验收（已随 PR #56 合并 main，merge commit `0b125319f8b2ef4f05428d75925e8293c3e8d4a6` 本地 git 可验证，PR CI run 回填待后续状态切片——`tools/harmony_mock` 复用 Android 只读 mock 契约，仅暴露 Home 六个 GET 端点，非 GET/未支持 method（如 HEAD/OPTIONS/FOO）/未知路径/错误 audit query 均 404；契约测试实现时首验 18765 与 28765 均 15/15 passed，审查补齐未支持 method 负例并修复 fail-closed 501 缺口后最终两端口均 18/18 passed；Settings 真实 UI 保存并测试 `http://192.168.8.3:8765/` 成功，Home 六区 mock 渲染通过；停止 mock 后六区错误态保留重试、App 不崩溃且四 Tab 可切换。已知 HomePane 仅 aboutToAppear 读取一次 base URL，Settings 更新后已挂载 Home 不自动刷新，后续独立优化（该缺口已由 M13-03 修复）。证据 README 与 fixture 入库，`.verify/` 原始证据不入库；未验真机、真实 provider、生产后端/生产 DB，CI 无 HarmonyOS job，production_ready=false 不变）
- [x] M13-03 Home 地址变更自动刷新（已随 PR #57 合并 main，merge commit `529b4d4ade7d172cbc3c4d66bcf0720568bbb9a3`——修复 M13-02 记录的 HomePane 生命周期缺口：Settings 保存 base URL 成功后，已挂载 Home 无需重启即自动刷新。使用官方 `@kit.BasicServicesKit` `emitter` 进程内事件；事件 ID 集中由 SettingsStore 导出 `EVENT_ID_URL_CHANGED = 'aios://settings/url_changed'`；SettingsStore 仅在 UrlPolicy 校验 + preferences `put` + `flush` 全部成功后 emit，非法保存不写盘不发事件；HomePane 持单一稳定回调，`aboutToAppear` 先 off 再 on、`aboutToDisappear` 用带回调的 `emitter.off` 精确退订；回调不信任事件 payload，以 `loadBaseUrl(context)` 重读持久化为权威来源；`aboutToAppear` 仍仅一次初始加载与一次初始刷新。clean 构建通过（`hvigorw.bat clean --no-daemon` → `assembleHap --no-daemon` 均 BUILD SUCCESSFUL，HAP 197,338 bytes，SHA256 `C06327CED5973DD5A634E8A974C2DF9C26D6991CBF815F2BE9416BC8C1938960`）；本地模拟器 `127.0.0.1:5557` 正向流（默认地址真实错误态 → Settings 真实 UI 保存 `http://192.168.8.3:8766/` → 不重启 App，Home URL 与六区刷新为 mock 数据）与负向流（`javascript:alert` 被 URL policy 拒绝后 Home 保留旧数据不刷新为错误态，证明无事件发出）均通过；证据 README 入库，`.verify/` 原始证据不入库；PR head `b5a60a1` CI 四项 success，merge 后 main CI run `34157243447` 四项 success；未验真机、真实 provider、生产后端/生产 DB、治理写链路，CI 无 HarmonyOS job，production_ready=false 不变）
- [x] M13-04 Android USB 物理设备冒烟（已随 PR #58 合并 main，merge commit `029281639ba645742d9ea089b96f2493774b8b68`——M12-06 冒烟 harness 扩展到 USB 物理设备：`--device-type physical` 在 App 启动前 `adb reverse --no-rebind` 建立设备回环→宿主回环通道（mock 仍只绑宿主机 `127.0.0.1`，不暴露 LAN），首次启动后经真实 Settings UI 把 base URL 配成 `http://127.0.0.1:<port>/`，收尾 finally 中 `adb reverse --remove`（失败如实记 stage）；滚动坐标按 `wm size` 实测尺寸百分比换算（1080x2400 等价旧坐标、720x1600 落屏内），搜索流程执行后按需收起 IME（`dumpsys input_method` 判断）再滚结果卡进视口；finalize 韧性——dump logcat 失败（如 USB 掉线）时 dump-logcat 记 failed、stats 置零标 `unavailable`、summary 照常写出，不隐性通过；真机加固——uiautomator dump 零尺寸 bounds 节点跳过、ANR 词边界匹配（`fileCanRead` 子串不再误判）。Python 单测 285 passed / 1 skipped（canonical venv）、Gradle `testDebugUnitTest` 347/0 + `lintDebug` 0 error + `assembleDebug`、模拟器回归 15/15 stage；真机 Huawei MGA-AL00（720x1600，serial `EYFBB22923201473`）18/18 stage passed、mock 19 expected / 0 unexpected / 19 total、logcat FATAL/ANR/本包 crash 均 0、`adb reverse --list` 收尾为空；PR CI run `34160683530` 与 merge 后 main CI run `34176554925` 四项 success（证据 `docs/evidence/m13-04-android-physical-smoke/`）；mock only、无真实 provider/生产后端/生产 DB/凭据，production_ready=false 不变）
- [x] M13-05 Study 论文只读列表（✅ 2026-09-08 本地实现与本地模拟器验收——已随 PR #60 合并 main，merge commit `34d30ec4d9afee15dda037713ab9867f3e33d1f8`（merge 后 main CI 四项 job 全绿）：`AiosApi` 新增第 7 个只读端点 `GET /api/v1/papers` 与 `PaperOut` DTO（仍硬编码仅 GET）；`StudyPane` 静态占位升级为 LOADING/EMPTY/SUCCESS/ERROR 单一状态机（EMPTY=HTTP 成功零条、ERROR 原始错误+重试、SUCCESS 渲染存在字段+刷新、空可选字段省略渲染），订阅既有 `aios://settings/url_changed` 事件以重读持久化地址自动刷新；共享 mock 契约新增确定性 3 篇论文 fixture（paper-003 `origin_url: null`），GET 200 / POST 404；`tools/harmony_mock/test_contract.py` 20/20 passed、`pytest tests/android_smoke/test_mock_contract.py tests/android_smoke/test_server.py` 28 passed；hmharness 稳定性修复（元数据 Flex(Wrap) 换行、`disposed` 守卫、请求代际守卫）后最终复验构建 clean `assembleHap` 通过（最终 HAP 255674 bytes，SHA256 `3E3CF7CBB3160F15FE8A78240F24F4D1036AC6771E90C42E962DC532CB958E0D`；初始 Stage 3 HAP 240231 bytes、`E4255F488C06FAB755F0CA844F407E077533332485737E7EDE1D9DD50FD36B3A`；已知警告同基线：无显式 targetSdkVersion、无签名配置、SettingsStore may-throw）；模拟器 3A 默认地址真实错误态 / 3B Settings 保存后不重启刷新三篇论文 / 3C 停服错误→重启恢复全通过，最终 supervisor 复验（fresh install PID 15016）全流程再次通过（证据 `docs/evidence/m13-05-harmony-study-papers/`，`.verify/` 原始证据不入库）；空态与滚动未 UI 覆盖（mock 恒返 3 篇）、`[object Object]` 错误文案为 UX 跟进；未验真机、真实 provider、生产后端/生产 DB/写链路，CI 无 HarmonyOS job，production_ready=false 不变）
- [x] M13-06 Search providers 只读列表（✅ 2026-09-08 本地实现与本地模拟器验收——已随 PR #61 合并（PR head 902fbda，PR CI run 34218137511 四项 success；merge commit 8d4445a，merge 后 main CI run 34238561233 四项 success），基于 `origin/main@34d30ec4d9afee15dda037713ab9867f3e33d1f8`（PR #60 merge commit）：仅 `GET /api/v1/search/providers` 一个只读端点（M12-04 其余 search 端点 plan/queries/queries/{id} 对 Harmony 一律 404），`AiosApi` 新增第 8 只读端点与 `SearchProviderOut` DTO/`getSearchProviders`，`SearchPane` 占位升级为 LOADING/EMPTY/SUCCESS/ERROR 状态机（订阅 URL 变更自动刷新、`disposed`/请求代际守卫、`unavailable_reason` 非 null 才展示），mock 契约 fail-closed 收紧后 20→30 项；`tools/harmony_mock/test_contract.py` 30/30 passed（127.0.0.1:18765）、Android 冒烟单测 287 passed / 1 skipped；clean `assembleHap` 通过（未签名 HAP 298153 bytes、SHA256 `7D31E3F43F4A2547F2299184D64AA14ADC8735758963F362B1DA46643BF23297`；已知警告同基线：无显式 targetSdkVersion、SettingsStore may-throw、未签名）；模拟器 Stage A 默认地址真实网络错误态 / Stage B Settings 保存后不重启显示 local-corpus（启用）与 cloud-web（禁用+原因）/ Stage C 停服错误→重启恢复全通过，App PID 28168 全程稳定、收尾 8766 端口监听数 0（证据 `docs/evidence/m13-06-harmony-search-providers/`，`.verify/` 原始证据不入库）；已随 PR #61 合并（PR CI/main CI 两轮四项 success）；未验真机、真实 provider、生产后端/生产 DB/写链路，CI 无 HarmonyOS job，production_ready=false 不变）
- [x] M13-07 Voice providers 只读面板（✅ 2026-09-09 本地实现与本地模拟器验收——已随 PR #63 合并 main（merged_at `2026-09-08T16:45:25Z`，PR head `d201b062f86a2179fa5d8421053e4ece478971a7`、merge commit `6c72c75dc3f09e9aeb29f683554f00410102075d`，本地 git 可验证；PR CI run `34252437713` 与 merge 后 main CI run `34253004195` 四项 success）），基于 `origin/main@5ef95b29872bf5e50d839ef480f98093f8c195ec`（PR #62 merge commit）：仅 `GET /api/v1/voice/providers` 一个只读端点（其余 voice 端点 token/sessions/transcribe/synthesize/trace 对 Harmony 一律 404，含 GET 形式），`AiosApi` 新增第 9 只读端点与 `VoiceProviderViewOut`/`VoiceProvidersData` DTO/`getVoiceProviders`，`VoicePane` 占位升级为 LOADING/SUCCESS/ERROR 状态机（订阅 URL 变更自动刷新、`disposed`/请求代际守卫、响应字段类型校验、fallback「已回退」徽标、requested 非 null 才展示、隐私开关仅事实标签），mock 契约 30→42 项；`tools/harmony_mock/test_contract.py` 42/42 passed、`pytest tests/android_smoke/test_mock_contract.py tests/android_smoke/test_server.py` 32 passed；clean `assembleHap` 通过且重复声明行删除后 hmharness 与 Codex 双重独立复验 BUILD SUCCESSFUL（最终 HAP 348946 bytes、SHA256 `970241F92ECE4736D90D8A7ACE9025FD9D2548FA7991911172353DB54CC5C283`；初始 Stage A/B/C 产物 347823 bytes、`1BF9B0BC…`；已知警告同基线：无显式 targetSdkVersion、SettingsStore may-throw、未签名）；模拟器 Stage A 默认地址真实网络错误态（PID 19960）/ Stage B Settings 保存 `http://192.168.8.3:8766/` 后不重启显示 voice_mode=hybrid、ASR fake、TTS tone+cloud-openai-tts+「已回退」、隐私关/开 / Stage C 停服约 33 秒错误→重启恢复全通过（PID 19960 全程稳定），最终产物重装复验（PID 8444）正向渲染逐项一致，收尾 8766 端口监听数 0（证据 `docs/evidence/m13-07-harmony-voice-providers/`，`.verify/` 原始证据不入库）；停服约 33 秒错误时延与 `[object Object]` 错误文案记为非阻塞 UX 加固跟进项（不计为已通过的 UX 质量项）；未验真机、真实 provider、生产后端/生产 DB/写链路/任何真实语音能力（ASR/TTS 执行、麦克风），CI 无 HarmonyOS job，production_ready=false 不变）
- [x] M13-08 考试会话只读快照（✅ 2026-09-09 本地实现与本地模拟器验收——已随 PR #65 合并 main，PR head `9e1e64fe0c3bd89dbd3fc03f85a221d0a48fa9b9`、PR CI run `34284891184` 四项 success；merge commit `31b626926450618468dbe5c10064b6a6eb6f4506`，merge 后 main CI run `34285331863` 四项 success；远端功能分支已删除；本地提交 `47ad110`/`0d295fe` 的远端等价提交 `c0806267854fcddfc5a6084bae0342cb461b4a75`/`9e1e64fe0c3bd89dbd3fc03f85a221d0a48fa9b9` tree 逐字节一致（`da7dc8af2743a3beebd9fd64d8ba12aa64fa0b8a`/`d777ff02e206b3b1e359cfed43c99d12a7227190`，本地 git 可验证）），基于 PR #63 merge commit `6c72c75d` 之上的本地 M13-07 状态回填提交 `be97a72`（本地 git 可验证）：仅 `GET /api/v1/exams/{exam_id}` 一个只读**动态路径**端点（其余 exam 端点 POST 建考/PUT 作答/POST 提交/GET submission/report 对 Harmony 一律 404），`AiosApi` 新增第 10 只读端点与 `ExamSessionData`/`PublicQuestionOut`/`QuestionOptionOut` DTO/`getExamSession`（examId 白名单校验：trim/非空/≤128/拒控制字符/拒 `/?#`/仅 `[A-Za-z0-9._:-]`，不合法不发起请求，再 `encodeURIComponent` 编码；单次 GET、无重试/写入/凭据），`StudyPane` 论文区（M13-05）行为保持不变、新增完全独立考试只读区：独立 IDLE/LOADING/SUCCESS/ERROR 状态机（独立请求代次 + `disposed` 守卫、每次查询/重试恰一次请求、契约形状校验 fail-closed、公共元数据/题目/选项渲染、已保存作答明确标注非正确答案、无作答控件/提交/倒计时/正确答案/解析），URL 变更时考试区立即失效（清空旧数据回 IDLE）+ 中文重查提示、不自动重查，论文区照旧自动刷新；mock 契约 42→54 项；`tools/harmony_mock/test_contract.py` 54/54 passed、全量 tests/android_smoke 295 passed / 1 skipped；clean 与 assembleHap 双 exit 0（HAP 418261 bytes、SHA256 `E8E8E4B1C9D9DC85D8212386ACD4F2DF17373FCE47AD0B7FC702BB6047AB6A3A`；已知警告同基线：无显式 targetSdkVersion、SettingsStore may-throw、未签名）；模拟器 Settings 精确保存 `http://192.168.8.3:8766/` 后不重启：论文三篇照旧渲染，考试区缺省 `exam-m13-08-001` 查询显示 paper-001「Attention Is All You Need」/ mode=exam / status=active / remaining=900 / next_sequence=2、两道 mcq 全 8 选项、q1 已保存作答 A 标注非正确答案、q2 如实未作答；停 mock（listeners=0）后重试如实网络错误+数据清空，重启 mock 重试完整恢复；URL 改 8767 立即失效旧会话+中文提示不自动重查，改回 8766 论文自动恢复而考试保持 IDLE；App PID 27738 全程稳定，收尾 8766 监听数 0（证据 `docs/evidence/m13-08-harmony-exam-readonly/`，`.verify/` 原始证据不入库）；未验真机、真实考试会话、生产后端/生产 DB/写链路，CI 无 HarmonyOS job，production_ready=false 不变）
- [x] M13-09 HarmonyOS 生产加固（✅ 2026-09-09 本地实现与本地模拟器验收——基于 `main@de5f41a`（M13-08 状态回填提交）；交付前加固 4 文件 +62/−45：`build-profile.json5` 显式 `targetSdkVersion: "6.1.1(24)"` 消除 Missing targetSdkVersion 告警（未引入任何签名配置/材料）；新增 `ErrorText.ets` 纯函数 `safeErrorText`（白名单提取 code/message、200 字符有界、兜底中文「未知错误」、绝不 JSON.stringify 整个异常）；`AiosApi.ets` 删除 errToString 根因 `[object Object]`、非 2xx 响应体不再透出（5xx/400 固定中文文案、其余仅状态码）；`SettingsStore.ets` 消除 may-throw 告警（可抛调用词法入 try/catch）、emit 单独包裹失败不回滚持久化成功；权限基线不变仅 `ohos.permission.INTERNET`。契约 54/54、全量 Android 回归 295 passed / 1 skipped 与基线一致、clean+assembleHap 双 exit 0、**最终警告仅 1 项**（未签名诚实边界，基线 3→1）；HAP 422721 bytes、SHA256 `D382D5969EF79513220E2741196EEEAAF920260268F4ED9E12CC9A311979B919`；运行时冒烟（hdc -t 127.0.0.1:5557，App PID 25301 全程稳定）：默认地址错误文案 `网络请求失败: 2300007 …`（M13-05 的 `[object Object]` 已消灭）、Settings 真实 UI 保存后不重启订阅者刷新三篇论文、停 mock 错误态 `网络请求失败: 2300028 Operation timeout` 数据清空不崩溃、重启 mock 重试完整恢复、收尾 8766 监听数 0（证据 `docs/evidence/m13-09-harmony-production-hardening/`，`.verify/` 原始证据不入库）；已随 **PR #67**「feat(harmony): harden production readiness」合并 main——PR head `998f701a6ccf8e4d2293807c892644266b8965b5`（tree `18bebe450952b9cf8598a260b7a31b4dcfb36abf`，本地 git 可验证），PR CI run `34293394598` 四项 job（API/Android/Docker/Web）全部 success；merge commit `4572551f8ce88b150c5cb9bf6a2d910af2ab18d6`，merge 后 main CI run `34293722342` 四项 job 全部 success（Web 1m15s、Docker 2m27s、API 3m49s、Android 3m26s）；远端功能分支 `feature/m13-09-harmony-production-hardening` 已删除；未验真机、真实 provider、生产后端/生产 DB，AGC 签名/HarmonyOS CI 仍缺位（CI 无 HarmonyOS job，两轮四项 CI 结果均不扩大为 HarmonyOS 远端验证），production_ready=false 不变）
- [x] M13-10 Harmony AGC 签名 readiness preflight（✅ 2026-09-09 本地实现——已随 **PR #69**「M13-10: Harmony AGC signing preflight」合并 main，feature commit `7342c519111893763d199c104aa3d22c403389d7`（chore: add harmony signing preflight，7 files +889：`tools/harmony_release/preflight.py` 签名前置 fail-closed 门禁 CLI、`json5lite.py` 最小 JSON5 兼容加载器、`tests/harmony_release/test_preflight.py` 35 项针对性测试、`.gitignore` 追加 `*.p12`/`*.p7b`/`*.cer`/`*.csr`/`*.jks` 材料防护、证据 README；`apps/harmony/**` 零改动），merge commit `bc41d6cf083191958ca9710ae5b71ba31e056aa5`（本地 git 可验证：parents 为 PR #68 merge commit `2fded00` 与 feature commit `7342c519`，merge 与 feature 的差异仅为 PR #68 的四份 M13-09 状态回填文档）；PR CI run `34296903848` 四项 job（API/Android/Docker/Web）全部 success（Web 1m25s、Docker 2m28s、API 3m49s、Android 3m36s），merge 后 main CI run `34297190550` 四项 job 全部 success（Web 1m26s、Docker 2m36s、API 3m45s、Android 3m25s）；远端功能分支已删除；本地验证 canonical venv `pytest tests/harmony_release` 35 passed、全量 tests/android_smoke 295 passed / 1 skipped 与基线一致、真实仓库 preflight `blocked_by_external_materials` exit 0；hmharness 复检（基于 feature commit `7342c519`，clean/release 两次构建均完成于 PR #69 合并之前；时点 worktree 相对远程跟踪分支 ahead 1 / behind 2）clean 与 release 构建均成功（0 ERROR、2 条预期 WARN；未签名 HAP 198569 bytes、SHA256 `D67FDA0B46018B30CD5F28A6D64BB320D592CE90246053E5102ABABF777490FD`，与首轮 hmharness 留存产物同字节数、不同哈希——符合 HAP zip 重建哈希不稳定的已知边界；两次 preflight 调用均 exit 0 `blocked_by_external_materials`；tracked 文件未变）（证据 `docs/evidence/m13-10-harmony-agc-signing-readiness/`，`.verify/` 原始证据不入库）；AGC 材料仍缺位、不存在已签名 HAP、未做真机/运行时验证，preflight 通过 ≠ 签名配置正确 ≠ 可发布，CI 无 HarmonyOS job（两轮四项 CI 结果不扩大为 HarmonyOS 远端验证），production_ready=false 不变）
- [x] M13-11/12/13/14 治理只读面板与运行时修正（已随 **PR #75** 合并 main——GovernancePane 只读视图：版本 / 运维快照 / 审计日志，无任何写操作；M13-14 结构性修正固定 header 于 Scroll 之外）
- [x] M13-15 Harmony 治理运行时验收（✅ 2026-09-13 本地模拟器验收，**mock-only**——`docs/evidence/m13-15-harmony-governance-runtime-acceptance/`：固定 header 在 load/reload/tab-return/error/recovery/deep-scroll 全状态可见；加载中刷新按钮可见但禁用（防重复刷新），加载/错误/恢复后重新启用；hilog 8538 行 0 FATAL/0 AppCrash/0 AppFreeze/0 JS_ERR；HAP 515871 bytes / SHA256 `a5829186e8c7f309f88d993db2b20ba5dfa02ce9db76e622183d849adff46095`；docs-only，零生产代码；production_ready=false 不变）
- [x] M13-16 Harmony 发布链五阶段 fail-closed 工具（✅ 已随 **PR #127** 合并 main（merge commit `6512987`）；feature commits：`a7553f4` preflight、`10b2c97` release build、`379f6c4` 签名包装、`5ae56e4` 签名校验、`9f86926` 设备冒烟、`38dca1a` layout 证据加固、`7c0f7c2` sign 路径收敛安全修正；PR CI run `35295051405` 五项 job（Docker/Release tools/API/Android/Web）全部 success；supervisor 门禁：tests/harmony_release 294 passed、compileall、ruff F/E9、git diff --check 全过；修正版模拟器冒烟（仅 `127.0.0.1:5555` + 未签名 HAP）：status ok/exit 0/mutation performed/6/6 commands/0 failures/cleanup ok/bundle uninstalled，layout node_count 2708/53798 bytes/SHA256 `D56825952B806EDDAC50F1AC794CD4C656C3354E60D91012CC5B883A76147E61`，HAP unsigned 452446 bytes/SHA256 `6389C7DF066635CCF274D27F918FC610963400AFD9871402D7EFE3DDABBC07E4`；M13-16a docs-only 证据回填 `docs/evidence/m13-16-harmony-release-chain/`；AGC 材料/真实签名/真机仍缺位，production_ready=false 不变）
- [ ] M13 后续切片（待评估：治理等其余业务域只读接入（学习域只读第一切片已由 M13-05 随 PR #60 合并交付、搜索域只读第一切片已由 M13-06 随 PR #61 合并交付、语音域只读第一切片已由 M13-07 随 PR #63 合并交付、考试域只读第一切片已由 M13-08 随 PR #65 合并交付，见上）、AGC 签名与发布流程（**发布链工具已交付**：readiness preflight 第一切片由 M13-10 随 PR #69 合并、完整五阶段链 preflight→release build→sign→verify→device smoke 已由 M13-16 随 PR #127 合并——fail-closed 工具、294 项测试与 `.gitignore` 材料防护；AGC 发布材料创建、真实签名接入与真机验证仍缺位，见上）、真机验证与真实 provider 冒烟——授权评估前不动工、不虚构进展）

## M14 生产语音与生产自愈（本机生产栈口径）

### M14-51 状态更新

- M14-51 交付审计归档就绪报告 CLI（开发切片，零生产触碰、零调度器读改、零 WORM/离线访问；分支 `ops/m14-51-audit-readiness-input` 基于 `main@965eb91`，单 local commit 不 push）：`tools/ops/audit_archive_readiness.py` 是 M14-50 已合并纯评估器 `audit_archive_scheduler.evaluate_readiness`（PR #132）的薄 CLI 封装（评估语义零新增零改动），补上「就绪评估只有库入口、无文件输入/报告输出命令」的可执行缺口。**local-only**：读两个本地 schema-v1 文档（fail-closed 只读——仅普通文件、1 MiB 上限读前后双检、严格 UTF-8 + 严格 JSON 拒重复键/`NaN`/非 object、输入绝不变异），就 `--now`（tz-aware ISO 8601，缺省当前 UTC）逐任务确定性分类，原子写 canonical 报告 + `<output>.sha256` sidecar（temp + `os.replace` 原子替换；输出、sidecar 及其 tmp 中间路径绝不覆盖任一输入；绝不创建目录；报告只记 role + basename + 字节数 + SHA-256，绝不记绝对本地路径）。退出码 `0` 成功生成报告（含 `due`/`overdue`/`blocked`）/ `2` 一切拒绝；零网络、零 env、零子进程、零 DB/S3（源码契约测试锁定）；FS 与时钟全注入。验证：聚焦 **44 passed**（FakeFS + `tmp_path` 双轨）、scheduler+readiness 联跑 **78 passed**、worm 邻居回归 **270 passed**、`ruff check services/api` 全过、`py_compile` 通过、`git diff --check` 干净、staged secret 扫描 0 命中。诚实边界：全部验证为合成文档 + 临时文件，未跑真实调度链路/真实锚链状态；不自动发现证据、不集成 Windows Task Scheduler、不接触 WORM/离线存储、不执行生产、不访问任何 provider；`production_ready=false` 不变。证据：`docs/evidence/m14-51-audit-readiness-input/README.md`（README 为唯一入库证据文件，原始验证不留产物）。

### M14-50 状态更新

- M14-50 Stage B（Harmony 模拟器交互验证）同样只做 docs-only 回填，零设备运行、零生产触碰、零代码改动；它把 supervisor 于 2026-09-18 11:54–12:01（GMT+8）在同一验证 worktree（detached `main@b9e8cc8`，起始 clean）完成的 Stage B 判定 **PASS** 转为可审计仓库证据：预构建未签名 HAP（188,984 bytes、SHA-256 `9d1b9609…34acb`，与 Stage A 冒烟同一产物）普通 `hdc install -r` 即成功（无签名、无绕行）；全程唯一目标 `127.0.0.1:5555`；PID 3198 单进程稳定无重启；六个 tab 经 `uinput -T -c` 真实点击、7 份 dumpLayout 哈希互异；治理「重试」触发全新 `GET /api/v1/version`（hilog 中 URL 以 netstack CHR 脱敏骨架呈现、明文 0 次出现，掩码逐位还原 + 时间戳/PID/GET 方法互证）；`127.0.0.1:8000` 在模拟器内解析到模拟器自身 → 全程预期 `2300007` 连接拒绝（只读失败态，未改 API base URL）；cppcrash/jscrash/appfreeze/FaultLog 全 0；卸载后 `bm dump` 与安装前逐字节相同（bundle 不存在验证）；模拟器保持运行。回填基线 `main@d613667`（PR #131 merge）；`b9e8cc8..d613667` 6 提交全为 docs + 归档调度器，对 `apps/harmony`/`tools/harmony_release` 区间 diff 为空。回填前独立复核：HAP certutil 重哈希一致；源证据 44 文件 / 4,190,967 bytes 逐文件 SHA-256 全锚定 + 聚合清单 SHA-256 `3ff6f7b5…9f09` 重derive 一致。诚实边界：无成功 API 数据渲染、无动画验证、无深层流程、无签名/AGC/Harmony 真机；`production_ready=false` 不变。证据：`docs/evidence/m14-50-harmony-emulator-stage-b/README.md`（原始转储/截屏/日志 gitignored 不入库，README 为唯一入库证据文件）。

- M14-50 只做 docs-only 回填，零设备运行、零生产触碰、零代码改动；它把 supervisor 于 2026-09-18 在两个独立验证 worktree（均 detached `main@b9e8cc8ec7a5e158f1e99468b60b24a27a648c19`，PR #129 merge）完成的移动端 current-main 双冒烟转为可审计仓库证据：Harmony 模拟器冒烟（HAP 188,984 bytes、SHA-256 `9d1b9609…34acb`、未签名；device smoke 6/6、cppcrash/jscrash/appfreeze/FaultLog 全 0、卸载后 bundle 不存在、模拟器保持运行；layout 与 M13-16 语义对比结构一致、仅时钟变化）与 Android USB 真机冒烟（Huawei MGA-AL00 serial `EYFBB22923201473`；APK 11,454,463 bytes、SHA-256 `da54763f…c570`；18/18 阶段、mock 契约 19 expected/0 unexpected/19 total、FATAL/ANR/本包 crash 全 0、`adb reverse --list` 空、App 保留）。回填基线 `main@15580f2`（PR #130 merge）；`b9e8cc8..15580f2` 为 docs-only（对 `apps/harmony`/`apps/android`/`tools/harmony_release` 区间 diff 为空），移动端源码与验证基线一致。回填前独立复核：HAP/APK 哈希与字节数实测一致，Harmony 清单 11/11、Android 清单 19/19 逐条重验全 OK。诚实边界：不声称 Harmony 签名/真机、真实 provider、生产就绪或 Android 真实 provider；单次冒烟 ≠ 长稳；`production_ready=false` 不变。证据：`docs/evidence/m14-50-current-main-mobile-smoke/README.md`（原始报告/截图/日志 gitignored 不入库，README 为唯一入库证据文件）。

### M14-49 状态更新

- M14-49 只做 docs-only 回填，零生产触碰、零工具重跑、零 G: 访问；它把 supervisor 已于 2026-09-18T02:51:22Z–02:52:06Z（UTC）完成的真实审计锚点 WORM 离线第二副本执行（源证据为主仓 gitignored `.verify/artifacts/m14-49-audit-worm-offline-copy/` 三份报告与 `.verify/m14-49-audit-worm-offline-execution/raw/` 原始快照）转为可审计仓库证据。工具 `tools/ops/audit_worm_offline_copy.py` 已随 **PR #129** 合并 main（merge commit `b9e8cc8ec7a5e158f1e99468b60b24a27a648c19`、feature commit `2461306`），执行基点即 `main@b9e8cc8`。三步闭环：preflight **pass**/problems=[]/existing absent → copy **pass**/problems=[]/`offline_verified=true`/`created=true` → verify **pass**/problems=[]/`offline_verified=true`/existing matching。离线根 `G:\AI-LearningOS-Audit-Offline\worm-root-v1`（根 marker `AIOS-OFFLINE-COPY-ROOT.marker`，SHA-256 `0fabda6d…15fa`，内容 + 单个尾随 `0A` 字节经 hexdump 证实）；布局 `audit-anchor/d2bfd877…73aa/` 下三文件哈希与源逐字节一致：`audit-anchor.jsonl` `d2bfd877…73aa`、`verify-report.json` `ff2494a8…cddd`、`manifest.json` `fc8886237ea7fb0045aba37bf177cf51167f419937b57e3fdab9d8139cf105f7`。原始快照 MANIFEST.txt 经独立核验：16 条目、0 哈希失配（MANIFEST 自身 SHA-256 `dca39fa2…be83`），secret 扫描通过。诚实边界：磁盘拓扑实证 G: 为**独立物理 Disk 1**（NVMe UNIS SSD S3 1TB，Healthy，Fixed NTFS）而 D:/E: 同属 Disk 0、F: 为 Ventoy 可移动介质（health warning，未用于本副本）——本副本是**独立物理磁盘上的、仓库之外的在线（online）第二副本**，不是真正断开（disconnected）/可移动轮换/离站（offsite）介质，不满足严格 3-2-1 口径的「1 份离线/离站」；单次复制 + 单次核验 ≠ 长期介质健康保障；真正离线/离站副本、定期审计锚定与归档调度（当前锚链仅 sequence 0 一条锚）、provider 冒烟、release-readiness/cutover 审批、长稳剩余面、AGC 签名发布链仍开放，`production_ready=false` 不变。证据：`docs/evidence/m14-49-audit-worm-offline-execution/README.md`（原始快照与报告本体 gitignored 不入库，README 为唯一入库证据文件）。

### M14-48 状态更新

- M14-48 只做 docs-only 回填，零代码改动、零设备操作、零生产触碰；它把 2026-09-17T23:51Z–23:56Z（UTC）完成的 Android USB 真机回归验证（验证-only，源证据为另一验证 worktree gitignored `.verify/m14-48-android-physical-regression/`）转为可审计仓库证据。回填分支基于 `main@1b35145`（PR #125 merge），验证基点 `main@c817ade`（验证分支头 = 基线、零新 commit），两基点间仅 docs 变更、Android 代码面一致——`gradlew.bat assembleDebug` exit 0，APK 11,454,463 bytes、SHA-256 `da54763fd63454aaa80f3d00dac1f1bffeec6d534070cd1d9a34bbb89687c570`，与 M13-04 归档 APK 逐字节一致（构建可复现）。设备：Huawei MGA-AL00（Android 10，720x1600，USB serial `EYFBB22923201473`）。r1 `--port 8000` 失败安全：宿主回环 8000 被用户进程占用（`WinError 10013`，按边界未停止该进程）→ exit 1、证据保留，bind 失败发生于 server 构造期，wait-device / adb reverse / install 均未执行——零设备副作用、无需清理；换 `--port 8100` 后 r2 通过：`overall_status=passed`（142.5s）、18/18 stage 全 passed、mock 契约 19 expected / 0 unexpected / 19 total（与 M13-04 r3 请求形态一致、零写方法）、logcat FATAL 0 / ANR 0（词边界）/ 本包 crash 0——`androidruntime_crash_count=52` 为含 "AndroidRuntime" 行的 uiautomator 工具进程 D 级诊断计数、非 App 崩溃，与 M13-04 已归档口径一致；收尾 `adb reverse --list` 为空、`pm clear` 已执行、App 保留设备。最终判定 PASS（8/8 判定维度，#6 error/recovery 附条件——直接验证的仅环境层：r1 端口冲突失败安全 + 换端口恢复，App 内异常分支未触发、仅由 M13-04 历史证据与单测基线间接覆盖）。诚实边界：本验证仅为 **USB 真机 loopback mock 回归口径**——mock 只绑宿主 `127.0.0.1` + `adb reverse` 通道，不构成真实 provider / 生产 API / 生产 DB / 云语音检索 LLM 的通过证明；真机 USB 有掉线史（M13-04 r1），本轮 r2 未发生；单次验证 ≠ 长期稳定，`production_ready=false` 不变。证据：`docs/evidence/m14-48-android-physical-regression/README.md`（截图、XML dump、原始日志与 mock 日志不入 git，README 为唯一入库文件）。

### M14-47 状态更新

- M14-47 只做 docs-only 回填，零生产触碰；它收口 M14-46 记录的「生产 web 容器未经浏览器验收」诚实边界，把 supervisor 已于 2026-09-18 04:56–05:02（+08:00）完成的生产 web 容器只读浏览器验收（源证据 `.verify/m14-47-production-web-container-acceptance/`，4 截图 + ACCEPTANCE-REPORT.md + MANIFEST.txt，全部 5 文件 SHA-256 回填前逐文件复核一致）转为可审计仓库证据。验收对象为已在运行的 web 容器 `aios-m14-03-production-rehearsal-web-1`（ID `5be2e19db2e7…94e3`，与 M14-46 时点同 ID；image `aios/web:m14-05-security`，Docker image `sha256:793060d…`，healthy）——未重建、未重启、未重配置。结果：HTTP `GET /` 200（17759 bytes）、`GET /login` 200（16170 bytes）；API 健康端点 `http://127.0.0.1:8000/health` 200（是 `/health`，不是 `/api/v1/health`）；真实 Chromium 桌面 `/`/`/login` 完整渲染且 pageerror 双 0；匿名 `GET /api/v1/auth/me` 两次 401（预期，无会话）；单次冷加载 404 资源消息未复现、不计入判定；mobile 390×844 双页均 200、渲染、pageerror 0、无水平溢出。诚实边界：仅覆盖 desktop+390px 匿名 `/`/`/login` 直接渲染——不含认证后流程/其他移动宽度/跨浏览器/真实用户负载；web 有意绑定 loopback `127.0.0.1:3011`，`192.168.8.3` 是 LiveKit media 绑定而非 web 绑定，LAN-public web endpoint 未开放未验收；不隐含任何 provider smoke pass；WORM 离线第二副本/定期归档/长稳就绪开放；休眠并行 compose 项目未删除；LAN IP 静态假设；release/cutover 未批；单次只读验收 ≠ 长期稳定，`production_ready=false` 不变。证据：`docs/evidence/m14-47-production-web-container-acceptance/README.md`（原始证据 gitignored 不入库，MANIFEST.txt 全量 SHA-256 锚定）。

### M14-46 状态更新

- M14-46 只做 docs-only 回填，零生产触碰；它收口 M14-45 记录的「postgres/redis label drift 未解决」诚实边界。根因：生产 postgres/redis 容器的 config-file/working-dir 不可变 compose 标签指向已删除 m14-06 worktree 的 infra 路径——但无行为/配置漂移（变更前 `up -d --dry-run` 六容器零 Recreate，config hash 与主仓 compose 对齐；区别于 M14-45 的 livekit bind-mount 实失效）。调和（2026-09-18 04:45–04:46 +08:00 两次连续受控操作，先 postgres 后 redis）：变更前 PostgreSQL 自定义备份 70861 bytes / SHA-256 `8E4F27EA…`；主仓 compose 受控最小范围重建 `-p aios-m14-03-production-rehearsal -f infra/docker-compose.yml --env-file infra/env.production-recovery --profile local up -d --no-build --no-deps --force-recreate`（不加 `--renew-anon-volumes`）→ postgres `d17d5a93a079…`→`f928410e404e…`（04:46:07 healthy）、redis `89f3ed0fd02a…`→`6006a4c551e1…`（04:46:38 healthy），标签均改指主仓；postgres 命名卷与 redis 匿名卷 `2d88095b8c2e…` 前后同 ID（数据保留）；api/web/minio/livekit 容器 ID 逐一不变；六容器终态 healthy、最终 dry-run 零 Recreate、compose ls 仅主仓 compose 在列。应用面回归：API /health 200 前后一致、unknown-user login probe 预期 401、临时表事务 create/insert/select/rollback 全过（ROLLBACK 不留数据）、redis PING→PONG、livekit LAN IP `192.168.8.3` 13 条端口拓扑不变。诚实边界：休眠并行 compose 项目未删除；不隐含任何 provider smoke pass；生产 web 容器未经浏览器验收；WORM 离线第二副本/定期归档/长稳就绪开放；LAN IP 静态假设；release/cutover 未批；`production_ready=false` 不变。证据：`docs/evidence/m14-46-compose-label-reconcile/README.md`（原始证据 gitignored 不入库，26 文件 MANIFEST SHA-256 `501edd6b…` 锚定）。

### M14-45 状态更新

- M14-45 只做 docs-only 回填，零生产触碰；它把 supervisor 已于 2026-09-18 完成的 LiveKit 主仓库受控恢复（源证据树 `.verify/m14-44-livekit-main-recovery/`）转为可审计仓库证据。根因：旧 livekit 容器 `49935f127ca0…` bind-mount 指向已删除的 M14-38 worktree YAML 路径（源路径变目录）→ OCI runtime "not a directory" ExitCode 127、7880/7881 无监听（API /health 200，其余面正常）。恢复（2026-09-18 04:25:09–04:25:10 +08:00 单次受控操作）：主仓库 compose 受控最小范围重建 `-p aios-m14-03-production-rehearsal -f infra/docker-compose.yml --env-file infra/env.production-recovery --profile local up -d --no-deps --force-recreate livekit` → 新容器 `4aa604546c80…` healthy、挂载改指主仓库 regular file、LAN IP `192.168.8.3` 上 TCP 7880/7881 + UDP 7882-7892 全 13 条发布（docker port + netstat 双确认），api/web/postgres/redis/minio 容器 ID 逐一不变。真实浏览器 default 模式验收 verdict=passed 13/13（token/connect/data/mic/cleanup、DOM 无 JWT、console 零错误、`ws://192.168.8.3:7880`）。诚实边界：生产 web 容器未重建未直接测试；LAN IP 静态、DHCP 变更仍是风险；postgres/redis label drift（指向已删除 m14-06 worktree 的 compose config-file/working-dir 标签）未解决；不隐含任何 provider smoke pass；单次恢复 + 单次验收 ≠ 长期稳定，`production_ready=false` 不变。证据：`docs/evidence/m14-45-livekit-main-recovery/README.md`（原始证据 gitignored 不入库，15 文件 SHA-256 锚定）。

### M14-44 状态更新

- M14-44 只做 docs-only 回填，零生产触碰；它收口 M14-43 旧清单中的「真实 WORM 归档执行」事实。supervisor 已于 2026-09-17 在真实 MinIO Object Lock 桶 `aios-audit-worm` 完成 `preflight -> archive -> verify`：源锚 `.verify/artifacts/m14-42-audit-chain-anchor/audit-anchor.jsonl` 为 354 bytes、SHA-256 `d2bfd877aa94632e6f68932a4d4bef8d963a6eb61aca12de6429c5fb7873aa4e`；对象 key `audit-anchor/<sha256>/audit-anchor.jsonl`、version `dc704b8d-6ebd-4acb-adb2-2135f89bb703`、`COMPLIANCE` 保留至 `2036-09-17T19:10:00Z`；三步均 pass，archive 与 verify 的 `worm_verified=true`，verify `problems=[]`。业务桶 `aios-objects` 未修改。Windows 工作区 tracked 锚文件因 CRLF 显示 355 bytes / 另一 SHA-256；Git blob 与 `.verify` 源文件仍为 354 bytes / `d2bf…73aa`，与 WORM 对象一致，不是对象漂移。离线第二副本、定期归档调度、provider smoke、release/cutover 审批、长稳剩余面与 AGC 签名链仍开放，`production_ready=false` 不变。证据：`docs/evidence/m14-44-audit-worm-execution/README.md`。

- [x] M14-01 本地真实语音部署第一切片（`feadd75` + `6089eaa` + `0503213` + `66eb080`，已随 **aef48f6**「feat(ops): production resilience recovery」汇总合并 main——local FunASR/CosyVoice adapter + 可复现 bootstrap，主 API 不嵌模型 SDK；详见 PROJECT_STATUS M14-01/02 条目）
- [x] M14-02 部署验收轮（本机真实部署 + compose 容器内端到端冒烟全过；单机口径，`production_ready=false` 不变——边界详见 PROJECT_STATUS M14-01/02 条目）
- [x] M14-03 确定性离线 wetext 缓存零网络复用（`78ce29e`，随 aef48f6 合并 main——wetext snapshot_download 绑定 `local_files_only=True` + gitignored artifacts 缓存预热；提交信息记录实测：第二次复用重启零文件下载且零 ModelScope HTTP 请求）
- [x] M14-04 voice_service_control 受控生命周期 + fail-closed 二轮（`a2b542c` + `6319eb0`，随 aef48f6 合并 main——manifest 即事实源、start 幂等、stop 绝不误杀（无 pkill/killall/按端口杀）；二轮三关 fail-closed：回环健康探测零代理 / 端口不符拒绝 / 仓库外 service-dir 拒绝）
- [x] M14-05 Next 16.3.4 安全升级（`5e47fd2` 原实现于 `fix/m14-05-web-next-security`，2026-09-11 rebase 至 `main@cae7aa0` 后以 head `539dbe1` 提交 **PR #76** 合并 main（merge commit `ebbb700`，本地 git 可验证）——清 next@15.5.24 内嵌 postcss 生产审计 1 moderate + 1 high（GHSA-qx2v-qp2m-jg93 等），升级后生产 audit 0 漏洞；生产 web 面已由 **M14-10** 升级至 `aios/web:m14-05-security`（运行时 Next.js 16.3.4，单机生产栈口径）；详见 PROJECT_STATUS M14-05/M14-10 条目）
- [x] M14-06 生产恢复编排 + `restart: unless-stopped` + Windows 登录自愈任务（R1–R2.1 随 aef48f6；R3 真实 Task Scheduler 回环适配随 **PR #78** merge `5bd3db7`；**2026-09-11 本机生产栈自愈验收通过**——`Start-ScheduledTask` 触发 13:03:39→13:03:47 Ready、LastTaskResult=0、pin 5/5、up 幂等 no-op、6/6 healthy、引擎未触碰、四端点 200、status=installed；详见 PROJECT_STATUS M14-06 条目与 `docs/evidence/m14-06-production-resilience/`）
- [x] M14-07 schtasks XML 字节编码回环修复（**PR #79** merge `832e5eb`，feature commit `556b35c`——`run_raw` 字节面四形态严格解码（UTF-16LE 无 BOM / LE BOM / BE BOM / UTF-8 `<?xml` 起始）；PR CI run `34564346333` 为外部基础设施/billing 形态失败（约 5 秒、5 job、0 step），非代码回归）
- [x] M14-09 Web 镜像 tag 独立发布/回滚锚点（分支 `fix/m14-09-web-image-tag` 基于 `main@539dbe1`，已随 **PR #81** 合并 main（feature `321fb50`、merge `433d018`，本地 git 可验证）——compose web 镜像改读独立 `AIOS_WEB_IMAGE_TAG`（默认 local，不跟随 `AIOS_IMAGE_TAG`）；recovery pin 扩六键（新增 `AIOS_WEB_IMAGE_TAG`，web 容器镜像独立在线事实）；`build_release_candidate.sh` 同 tag 显式双变量；该切片**不改变当时生产容器，只解锁后续 Web-only 升级**——合并后真实 env 补 `AIOS_WEB_IMAGE_TAG` 键与首次 Web-only 升级演练已由 **M14-10** 完成；详见 `docs/evidence/m14-09-web-image-tag/` 与 `docs/evidence/m14-10-production-web-upgrade/`）
- [x] M14-10 生产 Web-only 升级（M14-05 安全镜像上产）+ 状态回填（✅ 2026-09-11——生产 env 补 `AIOS_WEB_IMAGE_TAG=m14-05-security`（`AIOS_IMAGE_TAG=m14-03-prod-rehearsal` 不变，supervisor 先仓库外备份 gitignored 真实 env，仅此一次非密钥键变更），canonical compose `up -d --no-build --no-deps web` 只重建 web：旧容器 `3a9a271f…`（m14-03-prod-rehearsal）→ 新容器 `5be2e19d…`（`aios/web:m14-05-security`，healthy，运行时自报 Next.js 16.3.4，预检容器 / 与 /login 双 200）；api/数据面/语音引擎零触碰——api 容器 ID/启动时间部署后与恢复触发后两次核对不变，FunASR/CosyVoice unmanaged-running healthy（owner PID 1183/2061 不变，两次恢复均 leave/untouched，未重启）；升级后 Web `/` 与 `/login`、API/FunASR/CosyVoice `/health` 全 200、compose 6/6 healthy；恢复 dry-run 与真实 `Start-ScheduledTask`（2026-09-11 17:09:55 本地，Ready、LastTaskResult=0）双绿（六键 pin 6/6、`up` 幂等 no-op、语音 untouched）；PR #81 CI run `34582538887` 五 job 全败 `step_count=0` 为已知外部 GitHub Actions/billing 形态失败（非代码回归，CI 未运行不隐藏）；回滚锚点=`AIOS_WEB_IMAGE_TAG` 改回 `m14-03-prod-rehearsal` 重跑同命令；单机生产栈口径，`production_ready=false` 不变；证据 `docs/evidence/m14-10-production-web-upgrade/`）
- [x] M14-11 生产 soak/并发彩排 harness + 有界只读生产 soak 执行（✅ 2026-09-11——交付分支 `feat/m14-11-production-soak` 基于 `main@3634d90`，开发回合交付 harness + 61 项契约测试（开发回合零生产流量），随 **PR #83** 合并 main（merge `14d7e2f`、feature `bc3b5ce`，本地 git 可验证）；supervisor 于获准窗口执行三轮有界只读 soak **全部零失败**：120/300/2000 请求（concurrency 2/4/4），p99 12.929–13.362ms，吞吐 18.462–36.644 rps，零 `completed_after_deadline`，每目标全 200（40/40/40、100/100/100、667/668/665）；前后基线逐项一致（compose 6/6 healthy、五端点 200、api/web 容器 ID 与 started 不变、voice PID 1183/2061 不变、**零恢复任务触发**）、语音面零负载；PR CI run `34612290177` 与合并后 main CI run `34612382356` 均为已知外部 0-step 形态失败（5 job 全败 `step_count=0`，非代码回归，CI 未运行不隐藏）。`tools/ops/soak_rehearsal.py`：默认 plan 零网络；execute 五要素门禁（旗标 + 精确确认短语 `EXECUTE READ-ONLY LOOPBACK SOAK` + duration≤120s + concurrency≤8 + 总请求≤2000，超顶 fail-closed 零请求）；固定目标画像（Web 3011 `/`+`/login`、API 8000 `/health`；`--include-voice` 才加 8010/8011 低频 GET `/health`）；GET-only/unauthenticated/仅字面 loopback IP/`http.client` 直连零代理面；报告落 gitignored `.verify/artifacts/m14-11-production-soak/` 绝不入库（仅文件名+指标入档）。**仅闭环「有界本地彩排 soak」证据——>60s 长稳、真实客户端负载形态、跨机等仍属未决；`production_ready=false` 不变**；结果回填分支 `docs/m14-11-production-soak-results`；详见 PROJECT_STATUS M14-11 条目与 `docs/evidence/m14-11-production-soak/`）
- [x] M14-12 生产监控/告警 readiness 开发切片 + 生产监控执行结果回填（只读采集 + 阈值判定 + 证据报告，✅ 2026-09-11 工具交付与首次真实只读生产监控 + 2026-09-12 supervisor 评审 R1 修正与结果回填——分支 `feat/m14-12-production-monitoring-readiness` 基于 `main@2a33ac5`，本 Claude 开发回合仅做本地 commit（交付 + R1 追加 commit），supervisor 审查与 remote 发布在其后进行；已随 **PR #85** 合并 main（merged_at **2026-09-11T17:34:05Z**，merge `52980c637f4e39fec196a6563dbab1875faa6a8f`、feature head `80e599d07fb448766d97a105c99d83275a50784a`，本地 git 可验证；远端 feature 分支已删除）；PR CI run `34627846208` 与合并后 main push CI run `34628419347` 均全部 5 job（Web/API/Docker/Android/Release tools）SUCCESS——与 PR #78–#83 多轮外部 0-step 形态失败不同，本轮 CI 真实运行且全绿；结果回填分支 `docs/m14-12-production-monitoring-results`（基于 main@52980c6，docs-only，本 Claude 回合仅做本地 commit）。`tools/ops/production_monitor.py`：默认 plan 零 subprocess/零网络/零生产读取；execute 需旗标 + 精确确认短语 `EXECUTE READ-ONLY PRODUCTION MONITORING` + 全部阈值在硬顶内（R1 起非有限浮点 nan/inf/-inf 拒绝 + `--project` 严格白名单——ASCII 字母数字开头、仅字母数字/连字符/下划线、≤64，被拒值不回显），缺一即 EXIT 2 零采集；只读采集面固定画像（compose project `aios-m14-03-production-rehearsal`：compose ps + 六容器 inspect 五事实 + 五默认端点 GET（3011 `/`+`/login`、8000/8010/8011 `/health`）+ `docker logs --tail` 安全错误摘要——只记计数/级别/类别，原文绝不持久化）；一切 docker 命令经只读白名单门（仅 compose ps / inspect --format / logs --tail，mutation/交互形态在任何执行前拒绝）；loopback/GET-only/`http.client` 直连零代理面与 M14-11 同款；部分失败 `partial=true` + 缺失≠healthy → `overall_status=incomplete`；阈值含边界（compose 6/6、五端点 200、容器 health、RestartCount、日志错误、延迟 warn/critical），warn 恒可见；schema v1 JSON+MD 原子写（拒 symlink/越界 stem）默认落 gitignored `.verify/artifacts/m14-12-production-monitoring/`（`--artifact-dir` 自定义路径为操作者显式自选覆盖，位置与 gitignore 状态由操作者负责）；189 项契约测试（含 R1 修正回归）+ 邻居回归 soak 61/recovery 45/startup 66 全绿。**生产监控执行结果（supervisor 获准窗口，2026-09-11T17:39:20Z–17:39:21Z，给定事实如实回填；原始报告 gitignored `monitor-20260911-173920.json`（14185B）/`.md`（3330B）不入库）**：compose 六服务全部 running healthy 且 restart_count 全 0、五端点 GET 全 200（延迟 ms 7.088/12.909/12.388/24.444/27.641）、逐容器日志 error_total 全 0、`partial=false`、ok=34/warn=0/critical=0、`overall_status=ok`、`monitoring_ready=true`——**单次只读快照全绿 ≠ production_ready；`production_ready=false` 不变**（持续/定时采集与调度、外部告警接入、指标历史与留存、阈值随时间的标定、跨机监控仍未开放）；详见 PROJECT_STATUS M14-12 条目与 `docs/evidence/m14-12-production-monitoring/`）
- [x] M14-13 监控历史索引 + 有界留存 + 趋势摘要（✅ 2026-09-12 工具交付 + 合并 + 首次真实历史构建——分支 `feat/m14-13-monitor-history-retention` 基于 `main@d1d5019`，本 Claude 开发回合仅做本地 commit，supervisor 审查与 remote 发布在其后进行；已随 **PR #87** 合并 main（merged_at **2026-09-12T08:13:50Z**，merge `d2ffc49b2770891227b52aea0307fb969969c7e2`、feature head `b1201d6d041ae3ea492ee918d4572584a6c47b02`，本地 git 可验证；远端 feature 分支已删除）；PR CI run `34682507203` 与合并后 main push CI run `34682734884` 均全部 5 job（Web/API/Docker/Android/Release tools）SUCCESS；结果回填分支 `docs/m14-13-history-results`（基于 main@d2ffc49，docs-only）。`tools/ops/monitoring_history.py`（单文件纯标准库、零子进程/零网络/零墙钟）：只读发现 M14-12 `monitor-*.json` 工件（stem 严格白名单含日历合法性，被拒名不回显），严格 schema 校验（incomplete/partial/非有限延迟/时间戳与计数类型违规一律 fail-closed 输出零写入，与 canonical 工件真实 schema 逐字段对齐）；SHA-256 去重（同哈希保留确定性代表，同 (project, collected_at) 不同哈希 = 冲突拒绝）；(collected_at, stem) 确定性排序；保留最新 N（默认 500、硬顶 5000）+ 显式 omitted_older_count 与 oldest/newest 边界，**源工件永不改动/删除**；输出原子 `history.jsonl`（紧凑记录，绝无原始日志行/密钥）+ `history-summary.md`（状态计数/availability/degraded、逐端点延迟 min/p50/p95/max、逐服务 restart/error 总计），**生成时间戳取自最新源样本——零墙钟、逐字节可复现**；symlink 全路径拒绝；一切 I/O 经 Store 注入。73 项契约测试 + 组合回归 262 passed（M14-12 套件零回归）。**首次真实历史构建（2026-09-12，回填回合从 canonical main `d2ffc49` 独立执行，连跑两遍逐字节相同）**：源 `monitor-20260911-173920.json`（SHA-256 `740c031e…4a458`，运行前后不变）→ 恰 1 行 `overall_status=ok`/`partial=false`/`artifact_sha256` 哈希链匹配；`history.jsonl` SHA-256 `a71ff7e2…ac5ae0`、`history-summary.md` SHA-256 `80535c3a…1a7be`（均与 supervisor 期望值一致）；ok=34/warn=0/critical=0、六服务 healthy+restart 0、五端点全 200、日志 error 全 0；输出 gitignored 不入库。**工具交付并合并；首次真实构建已执行（单样本全绿）；`production_ready=false` 不变**；详见 PROJECT_STATUS M14-13 条目与 `docs/evidence/m14-13-monitoring-history/`；同分支追加 M14-13 CI R2 commit（MinIO 社区镜像停发致 Docker CI 拉取失败 → 本地自建 pin 源码镜像 `infra/minio/Dockerfile`：codeload 官方不可变 commit URL + builder/runtime digest pin + 零 apk add + 非 root + compose 切换 + BusyBox wget 就绪探测 `/minio/health/cluster` + 21 项静态契约测试，零容器操作、镜像首次构建留给 CI（已随 PR #87 的 CI docker job 完成首次真实构建并转绿），production_ready=false 不变，详见 `docs/evidence/m14-13-minio-selfbuild/`））
- [x] M14-14 持续/定时监控采集 + 历史管道 readiness（✅ 2026-09-13 工具交付，**已随 PR #89 合并 main**（merged_at 2026-09-12T17:28:55Z，merge commit `f6f0356`，feature head `23a0390`；PR CI run `34708110486` 与合并后 main CI run `34708350434` 均全部 5 job SUCCESS；合并 = 代码入库 + CI 绿，零注册/零真实执行边界仍成立）——分支 `feat/m14-14-monitoring-scheduler` 基于 `main@1da4410`（PR #88 merge）。`tools/ops/monitoring_pipeline.py`：默认 plan 完全惰性（Runner 零构造）；execute 需 `--execute` + 精确短语 `EXECUTE READ-ONLY MONITORING PIPELINE`（缺一/近似 EXIT 2 零构造零调用）；**固定命令白名单门**——仅 monitor（`--execute --confirm "EXECUTE READ-ONLY PRODUCTION MONITORING"`，与 monitor 短语逐字一致回归锁定）与 history（全默认）两精确形态，无 shell=True/无用户注入面/子进程输出绝不持久化；序列 monitor → history（history 仅在 monitor exit 0 后运行，失败如实保留不遮蔽）；有界超时（monitor 60–540s 默认 480s、history 10–120s 默认 45s，硬顶和 660s < PT12M 时限 < PT15M 间隔——调度器绝不先杀整任务）；fail-closed 重叠锁 `pipeline.lock`（O_CREAT|O_EXCL，零 stale-lock 清理）；schema v1 原子报告仅安全事实（无绝对本机路径/脱敏错误类别类名/产物名+SHA-256+字节数）。`tools/ops/monitoring_pipeline_task.py` + `run_monitoring_pipeline_silent.vbs`：固定身份 `AIOS-Monitoring-Pipeline`（与 M14-06 任务零冲突）+ PT15M 保守重复间隔 + IgnoreNew/Hidden/InteractiveToken/LeastPrivilege；plan/generate/status/install/uninstall，**install/uninstall 各需精确短语 `EXECUTE MONITORING SCHEDULER CHANGE`**，结构性 schtasks 白名单门（读路径零 mutation；mutation 仅两精确 create/delete 形态、create 绝不 /F、绝不覆盖同名、uninstall 仅 exact-owned）；**实际注册 supervisor-only——开发回合零安装零注册零真实管道执行**。契约测试 52+79 项（FakeRunner/FakeSchtasks/临时目录，含 monitor/history/startup-task 既有契约回归 pin、VBS 契约与 generate 产物 UTF-16 BOM 字节级回归——supervisor R2 修正：旧实现 UTF-8 字节配 UTF-16 声明致 System.Xml 拒载，修正后与声明及 install 临时字节一致；PR #89 CI R3 修正回归：create 白名单 /XML 后唯一 token 是值位置——POSIX tempfile 绝对路径（Linux CI 形态）放行，旗标形态（-F//F/x.xml/F/非 .xml/多余/错位 token）仍逐项拒绝）；邻居回归 history 74/monitor 189/startup-task 66 全 passed（合并 460）。**readiness ≠ 持续运行证明；TimeTrigger/Repetition 归一化待首注册实证；`production_ready=false` 不变**；详见 PROJECT_STATUS M14-14 条目与 `docs/evidence/m14-14-monitoring-pipeline/`）
- [x] M14-15 监控历史洞察/告警摘要第 1 切片（✅ 2026-09-13 工具交付，**已随 PR #90 合并 main**（merged_at 2026-09-12T18:10:07Z，merge commit `ce10060`，feature head `acdbbb5`；PR CI run `34710154644` 与合并后 main CI run `34710372281` 均全部 5 job SUCCESS；合并 = 代码入库 + CI 绿，仍不接外部告警）——分支 `feat/m14-15-monitoring-insights` 基于 `main@f6f0356`（PR #89 merge）。`tools/ops/monitoring_insights.py`：M14-13 `history.jsonl`（或 M14-12 monitor 工件目录——校验/去重/排序**委托 M14-13 已测函数**，schema 单一事实源）→ 安全 `insights.json` + `insights-summary.md`（默认 gitignored `.verify/artifacts/m14-15-monitoring-insights/`，默认源位 `.verify/artifacts/m14-13-monitor-history-retention/` 为本切片指定新输入位、仓库现有工具尚无写入者）；洞察最小集：时间范围/样本数/时长、overall_status 计数、availability（计数+比率）、degraded/critical 事件列表（非 healthy 服务+非 200 端点明细，有界保最新、截断计数显式）、逐端点延迟 min/p50/p95/max（nearest-rank）+非 200 计数、逐服务 restart/日志 error/非 healthy 汇总、连续失败/恢复（当前连胜/最长 non-ok 连败/失败恢复转移/逐端点当前连败）、最近样本状态；fail-closed（行序乱序/重复/混档/超 5000/非有限延迟/部分失败形态一律拒绝且输出零写入）；默认 plan 完全惰性（零读取零写入零 Store 构造），execute 需 `--execute --confirm "EXECUTE READ-ONLY MONITORING INSIGHTS"` 一字不差；零子进程/零网络/零墙钟（两遍逐字节相同）；**stdout/输出绝无绝对本机路径/原始日志/密钥/生产容器 ID**。开发回合零生产执行、零 canonical `.verify` 触碰；验证 = 聚焦契约测试 **90 passed** + M14-13 回归 **164** + 监控全家族五套件 **484** + ruff/py_compile/`git diff --check` 全过 + 合成数据端到端演示（plan+execute 全默认、确定性）。**诚实边界：仅本地只读工件洞察，不接外部告警，不构成 production readiness——`production_ready=false` 不变**。详见 PROJECT_STATUS M14-15 条目与 `docs/evidence/m14-15-monitoring-insights/`）
- [x] M14-16 CosyVoice bootstrap 依赖解析降级回归修复（✅ 2026-09-13 契约修复，**已随 PR #91 合并 main**（merged_at 2026-09-12T18:28:09Z，merge commit `d6d0635`，feature head `f65251f`；PR CI run `34711072494` 与合并后 main CI run `34711281622` 均全部 5 job SUCCESS；证据 `docs/evidence/m14-16-cosyvoice-runtime-consistency/`）——分支 `fix/m14-16-cosyvoice-runtime-consistency` 基于 `main@ce10060`（PR #90 merge）。真实生产冷启动实证：最小运行时清单（`lightning==2.2.4` 官方 pin 链）按 PyPI 解析把 torch 降级到 2.3.1 而留下预装 torchaudio 2.11.0+cu128——cu128 轮 METADATA 不声明 torch 约束，**`pip check` 对该混合 ABI 报「No broken requirements found」不可信**，`import cosyvoice` 在 torchaudio `_extension` 崩 `undefined symbol: aoti_torch_abi_version`。`bootstrap_cosyvoice_wsl.sh` 修法：清单分支后从同一 cu128 index `--no-deps` 回写已验证三件套 pin（torch/torchaudio `2.11.0+cu128` + torchcodec `0.11.1+cu128`，已满足即 no-op，不 force-reinstall）+ 回写后、import 探针前新增运行期一致性探针（torch/torchaudio 基础版本一致 + 双 `+cu128` + torchcodec 可导入，fail-closed）；契约测试新增顺序锁定（初始安装 → 清单 → 回写 → 探针 → import/WAV）。验证 = 聚焦 `test_voice_local_scripts.py` **31 passed** + `test_voice_service_control.py` **64 passed** + ruff + `bash -n` + `git diff --check` 全过。**本切片只修复可复现 bootstrap 契约：生产 venv 修复与冷启动复验由 supervisor 合并后受控执行，`production_ready=false` 不变**。详见 PROJECT_STATUS M14-16 条目）
- [x] M14-17 CosyVoice bootstrap CUDA 闭包终局修复（✅ 2026-09-13 契约修复，**已随 PR #92 合并 main**（merged_at 2026-09-12T19:12:05Z，merge commit `5fbeb22`，feature head `74d3988`；PR CI run `34713163316` 与合并后 main CI run `34713465954` 均全部 5 job SUCCESS；证据 `docs/evidence/m14-17-cosyvoice-cuda-closure/`）——分支 `fix/m14-17-cosyvoice-cuda-closure` 基于 `main@d6d0635`（PR #91 merge）。生产实证（2026-09-13，**只读**取证生产 venv）：M14-16 的 `--no-deps` 终局回写只恢复三个主轮——torch/torchaudio/torchcodec 均已 cu128，但 `pip check` 报 nvidia-cudnn-cu12 8.9.2.26（torch metadata 需 `==9.19.0.56`）、nvidia-nccl-cu12 2.20.5（需 `==2.28.9`）、triton 2.3.1（需 `==3.6.0`），多数 CUDA runtime 仍是 12.1 系列（清单分支装 torch 2.3.1 时连带降级的闭包），`import torch` 失败缺 `libcudnn.so.9`；根因 = `--no-deps` 不恢复 torch metadata 声明的 Linux CUDA 依赖闭包。`bootstrap_cosyvoice_wsl.sh` 修法：终局改为**精确 pin 三件套 + 真实 METADATA 导出的闭包成员**（`cuda-toolkit[cublas,cudart,cufft,cufile,cupti,curand,cusolver,cusparse,nvjitlink,nvrtc,nvtx]==12.8.1` / `cuda-bindings>=12.9.4,<13` / `nvidia-cudnn-cu12==9.19.0.56` / `nvidia-nccl-cu12==2.28.9` / `nvidia-cusparselt-cu12==0.7.1` / `nvidia-nvshmem-cu12==3.4.5` / `triton==3.6.0`）的完整依赖解析（同一 cu128 index，无 `--no-deps`——精确 pin 防 PyPI 清单分支再降级，成员 pin 不满足即强制解析，可自愈存量破损态）+ `pip check` **附加门禁**（fail-closed 指向「CUDA closure 未恢复」；注释/测试明确它只是附加门禁——实证两盲区：混合 ABI 报 No broken requirements、extras 门控的 12.8 系列 runtime 错配不报，不能替代 import 探针）+ 一致性探针扩展 **18 项 CUDA closure 契约表**（== pin 精确相等 / extras 通配前缀 / cuda-bindings 范围，逐项点名差异）；契约测试改写顺序与内容锁定并新增闭包契约测试，探针已在生产 venv 只读实跑验证 fail-closed 点名（cudnn 8.9.2.26 != 9.19.0.56，先于 torch import 触发）。验证 = 聚焦 `test_voice_local_scripts.py` **32 passed** + `test_voice_service_control.py` 回归 + ruff + `bash -n` + `git diff --check` 全过（canonical venv 解释器仅执行，零 canonical 检出改动）。**本切片只修复可复现 bootstrap 契约：生产 venv 实际修复（重跑 bootstrap）与冷启动复验由 supervisor 合并后受控执行，`production_ready=false` 不变**。详见 PROJECT_STATUS M14-17 条目）
- [x] M14-18 CosyVoice 最小运行时 openai-whisper triton 元数据冲突修复（✅ 2026-09-13 契约修复，**已随 PR #93 合并 main**（merged_at 2026-09-12T21:53:44Z，merge commit `cc782b0`，feature head `2185bdc`；PR CI run `34721050144` 与合并后 main CI run `34721233549` 均全部 5 job SUCCESS；其后 PR #73 合并产生 main `42653b7`、merge-main CI run `34726676218` 亦 5/5 SUCCESS；证据 `docs/evidence/m14-18-cosyvoice-whisper-triton/`）——分支 `fix/m14-18-cosyvoice-whisper-triton` 基于 `main@5fbeb22`（PR #92 merge）。生产实证（2026-09-13 `artifacts/voice/cosyvoice/service/service.log`）：M14-17 终局 CUDA 闭包恢复已成功（torch `2.11.0+cu128` / triton `3.6.0` 就位），但最小清单 `openai-whisper==20231117` METADATA 声明 `triton<3,>=2.0.0`（无环境标记）与闭包冲突——`pip check` 附加门禁被「openai-whisper 20231117 has requirement triton<3,>=2.0.0, but you have triton 3.6.0」卡死 FAIL；且清单分支装 20231117 时该约束触发 pip 回溯把 torch 一路降级（实证 2.14.0→…→2.3.1）。修法 = **升级 pin 而非绕过门禁**：`openai-whisper==20250625`（METADATA `triton>=2`（x86_64/linux 环境标记、无上界）与 cu128 闭包共存；运行时依赖集合与 20231117 完全一致，不替换 torch/CUDA 包——supervisor 只读 dry-run + 本回合 PyPI sdist PKG-INFO 直读复核）；CosyVoice 固定 commit `074ca6d` 用到的两个 API（`whisper.log_mel_spectrogram(audio, n_mels=128)` 与 `whisper.tokenizer.Tokenizer(encoding/num_languages/language/task)`）经 20231117 vs 20250625 逐字 diff **源码级不变** + 生产 venv 只读签名探针实跑通过；M14-17 门禁/闭包契约/一致性探针一字不动，反绕过路径（`--no-deps` 装清单 / 强制降级 triton / 改写 dist-info / 弱化 pip check）全被新契约测试锁定；官方 requirements 固定 commit 仍 pin 20231117——有意偏离并留档（20250625 PyPI 仅 sdist）。契约测试新增 3 项（pin + 证据锚点 / 全生效行反绕过 / whisper 可导入环境的两 API 签名回归，canonical venv 显式 skip）。验证 = 聚焦 `test_voice_local_scripts.py` **34 passed + 1 skipped** + `test_voice_service_control.py` 回归 **64 passed** + ruff + `bash -n` + `git diff --check` 全过（canonical venv 解释器仅执行，零 canonical 检出改动）。**本切片只修复可复现清单/bootstrap 契约：未重跑 bootstrap、未向任何 venv 装依赖、未启停任何进程/容器，生产实际升级与冷启动复验由 supervisor 合并后受控执行，`production_ready=false` 不变**。详见 PROJECT_STATUS M14-18 条目）
- [x] M14-19 CosyVoice bootstrap ModelScope 下载载荷过滤（✅ 2026-09-13 契约修复（本地 commit，待 supervisor 审查发布）——分支 `fix/m14-19-cosyvoice-download-filter` 基于 `main@cc782b0`（PR #93 merge），本 Claude 开发回合独占 worktree 仅一个本地 commit。根因（生产实证，2026-09-13）：模型下载为无过滤 `snapshot_download(model_id, local_dir=...)`——整仓 ≈9.85GB（ModelScope API 实测 19 文件），≈4.44GB 与所选运行时无关（`llm.rl.pt` 2.02GB 固定 commit `074ca6d` 全仓零引用 / `flow.decoder.estimator.fp32.onnx` 1.33GB 仅 `load_trt=True`（bridge 默认不启用）/ `speech_tokenizer_v3.batch.onnx` 0.97GB 仅 `online_feature` 训练单例（env `onnx_path` 未设）/ `asset/dingding.png`+`README.md`+`.gitattributes`+`configuration.json` 运行时零引用）。修法 = 新增 `tools/voice/cosyvoice_model_payload.py` 载荷契约模块（仅标准库，py3.11 测试 venv 与 py3.10 生产 venv 双侧导入实证）：`RUNTIME_PAYLOADS` 按 model_id 注册 **12 精确路径白名单 + 11 项 strict 必需集**（`required ⊆ allow` 匹配集强校验），bootstrap 下载段经它 fail-closed 解析后以 `snapshot_download(..., allow_patterns=[...])` 下发（modelscope==1.20.0 受支持参数——hub/snapshot_download.py 允许列表对全路径 fnmatch，断点续传/缓存命中语义不变），下载后逐一存在性校验必需文件（上游布局变更即点名失败）。**fail-closed**：未注册 model_id / 空·畸形白名单（含 str 形态）/ 必需集为空 / 必需件未被白名单覆盖一律 `PayloadContractError`（modelscope 空列表语义 = 不过滤 = 整仓下载，绝不静默放行）；选白名单而非 ignore 列表——上游新增文件默认排除。**CosyVoice-BlankEN/\* 经源码+实证属真正必需**（cosyvoice.py:200 override `qwen_pretrain_path` → `!new:` 急切构造 `Qwen2Encoder`（llm.py:229 `Qwen2ForCausalLM.from_pretrained`）+ `!name:` partial → `AutoTokenizer.from_pretrained`；2026-09-13 WSL 生产 venv 探针实证 BlankEN 缺 `model.safetensors` 即 `OSError`——任务书 optional 假设被推翻，保留下载；未来不需它的运行时在注册表显式省略即排除）。bash 侧四文件载荷判定（`model_payload_ready`）/ ModelScope 缓存回落 / wetext 预热（M14-03）/ CUDA 闭包链（M14-16/17/18）全部零改动。契约测试新增 6 项（必需件放行且被选中 / 无关资产排除 + 19→12 净效果 / BlankEN 显式选择语义 / fail-closed 四路径 / fnmatch 语义镜像 / bootstrap 调用形态与旧无过滤调用移除），TDD RED 先行（6 项在实现前全失败）后 GREEN。验证 = 聚焦 `test_voice_local_scripts.py` **40 passed + 1 skipped** + `test_voice_service_control.py` 回归 **64 passed** + ruff 全过 + `bash -n` + `git diff --check` + 生产 venv (py3.10) 导入/fail-closed 冒烟全过（canonical venv 解释器仅执行，零 canonical 检出改动）。净效果 = 新冷机下载 ≈5.4GB（**-45%**）。**本切片只修复可复现 bootstrap 契约：未重跑 bootstrap（生产下载进程原样运行未触碰）、未启停任何进程/容器/代理，存量模型目录的已下载多余文件（llm.rl.pt 等）不主动删除——下次冷启动起白名单生效；生产冷启动复验由 supervisor 合并后受控执行，`production_ready=false` 不变**。详见 PROJECT_STATUS M14-19 条目与 `docs/evidence/m14-19-cosyvoice-download-filter/README.md`）
- [x] M14-20 监控历史索引历史 incomplete 工件修复（✅ 2026-09-13 契约修复（本地 commit，待 supervisor 审查发布）——分支 `fix/m14-20-monitoring-history-fix` 基于 `main@4c9458f`（PR #95 merge），本 Claude 开发回合独占 worktree 仅一个本地 commit。根因（真实工件面实证，2026-09-13）：M14-14 管道定时运行后 monitor 已随 M14-19 栈修复产出完整 warn 报告并 exit 0，但默认源目录混有 2026-09-12 栈修复期间 monitor 自产的 31 份 `partial=true + overall_status=incomplete` 工件（cosyvoice-health 端点 failed——采集器事实合法含 failed，无法经 M14-13 完整校验），旧「任何 incomplete 一律整体拒绝」语义令 `monitoring_history.py` 每轮 EXIT 2（`overall-status`）零输出——**监控历史永久零索引**。修法（最小 robust）= `tools/ops/monitoring_history.py` 新增纯谓词 `is_recognized_incomplete`：识别**窄类** = 完整 monitor 身份（schema_version==1 ∧ tool ∧ milestone==M14-12 ∧ mode==execute）+ `overall_status=incomplete` ∧ `partial is True` 共现对（monitor 契约中二者恒共现：任一采集器失败 → partial=true → incomplete；ok/warn/critical 恒 partial=false）→ **整件跳过不入档**（`skipped_incomplete_count` 显式于 summary dict/`history-summary.md`/stdout；源文件绝不改动/删除；跳过件内容绝不进入任何输出——仅计数）；完整 ok/warn/critical 样本照常走原样严格校验；其余任何 incomplete/partial 形态（身份不符、incomplete+partial=false、完整状态+partial=true 等矛盾组合）仍 fail-closed（输出零写入）；候选全为 incomplete → 新固定词汇 `no-complete-sources` 拒绝；`build_samples(store, source) -> list` 签名保持稳定（monitoring_insights monitor 工件目录形态委托面——单一 schema 事实源，跳过语义同样生效）；`history.jsonl` 记录 schema 零改动（`HISTORY_SCHEMA_VERSION` 不变）。契约测试新增 9 项（真实形状历史 incomplete + 完整样本混档入档/跳过计数/源逐字节不变 / 全 incomplete 拒绝 / 识别窄类 ×5 身份不符仍拒 / 跳过件不参与去重计数 / 留存仅作用完整样本）+ insights 委托回归 1 项，TDD RED 先行（4 项行为测试在实现前全失败于根因症状）后 GREEN。验证 = 聚焦 `test_monitoring_history.py` **83 passed** + 邻居四套件（insights/pipeline/pipeline_task/production_monitor）**411 passed** + ruff + `py_compile` + `git diff --check` 全过 + **真实 canonical 源目录端到端实证**（39 工件 = 31 历史 incomplete + 8 完整：exit 0、8 条完整记录（1 ok + 7 warn）入档、跳过 31 计数显式、39 源文件 SHA-256 逐字节不变、两遍输出逐字节相同——本回合仅运行 history 工具只读面，零管道执行/零生产触碰）。**本切片只修复索引契约：不删除/不改写任何历史源工件（跳过而非清理是有意设计——源工件是 M14-12 只读证据面），`production_ready=false` 不变**。详见 PROJECT_STATUS M14-20 条目与 `docs/evidence/m14-20-monitoring-history-historical-incomplete/README.md`）
- [x] M14-21 监控洞察接入持续管道（monitor → history → insights 三步组合 + insights 默认输入对齐 history canonical 输出，✅ 2026-09-13 契约扩展——**已随 PR #98 合并 main**（merge commit `1b89d9182566ba0ff4afe8f02893acefae94fb43`，feature head `1f131aa`，合并后 main CI run `34737912550` 5/5 job SUCCESS；本地 git 可验证）——分支 `feat/m14-21-monitoring-insights-pipeline` 基于 `main@9977ece`（PR #97 merge），本 Claude 开发回合独占 worktree 仅一个本地 commit。动机（双路径事实源实证）：M14-14 管道定时持续更新 history（canonical 输出 `.verify/artifacts/m14-13-monitoring-history/`），但 sequence 固定仅 monitor→history 不更新 insights；而 `monitoring_insights.py` 默认 source 是 M14-15 切片指定的 `.verify/artifacts/m14-13-monitor-history-retention/`（仓库现有工具无写入者）——insights 永久无新鲜输入。修法 = ①`tools/ops/monitoring_insights.py` 默认 source 改为**直接引用同仓 `monitoring_history.DEFAULT_OUTPUT_DIR` 常量**（单一事实源，非平行定义；契约测试锁定 pipeline/history/insights 三方 resolve 全等）；②`tools/ops/monitoring_pipeline.py` 升级三步序列：insights 仅在 history status=ok 后运行，任何失败/跳过固定词汇原因入档（`<prev>-status-<status>`）且前置步骤退出码/类别如实保留绝不遮蔽（insights 失败不改变 monitor/history 事实）；③固定命令白名单新增**唯一** insights 精确形态 `--execute + "EXECUTE READ-ONLY MONITORING INSIGHTS"`（与 insights 自身门禁短语逐字一致，回归测试锁定）且**恒不带 --source**（canonical 输入经已修正默认值生效——无用户可注入 argv，`--source`/`--event-limit` 注入一律 CommandNotAllowedError）；④insights 有界超时 5–50s（默认 15s，纯本地只读工件处理秒级完成即兜底杀停），三步硬顶之和 540+120+50=710s < PT12M=720s 执行时限（monitor/history 界 60–540/10–120 与默认 480/45 不变——兼容面保留；`monitoring_pipeline_task.py` 注释与预算交叉 pin 测试同步三步口径）；⑤报告 config 增 `insights_tool`/`insights_timeout_seconds`/`insights_confirm_phrase`/三步 sequence，stages 增 insights 条目（产物两固定名 insights.json/insights-summary.md + SHA-256 + 字节数），schema_version 仍 1（加法演进）；安全面不变——无绝对本机路径/无子进程 stdout/stderr 原文/无 env/token、写前 redact_secrets 终防线。契约测试 TDD RED 先行（RED 实证：insights 默认源/预算链/README pin 4 项失败 + pipeline 套件 collection 即失败于缺失常量，全因功能缺失）后 GREEN：`test_monitoring_pipeline.py` 52→63（三步序列/白名单第三形态与注入拒绝/预算 710<720/短语 pin/路径三方一致/history 失败跳过 insights 原因可见/insights 失败不遮蔽/insights 产物发现与目录缺失如实/CLI 默认）+ `test_monitoring_insights.py` 91→93（默认源 == history canonical 输出 + README pin 更新）+ `test_monitoring_pipeline_task.py` 预算 pin 三步化（79 项）。验证 = 监控家族三套件 **238 passed**（RED→GREEN 全程）+ 监控全家族五套件 **510 passed** + 全量 services/api 套件 **2496 passed / 34 failed**（34 失败经主仓同 commit `9977ece` 基线对照（同 15 失败文件复跑 = 同样 34 failed/801 passed）确认为 main 既有失败——全部位于本切片零触碰的 evidence/release/governance/voice 环境敏感域，非本切片引入）+ ruff（`services/api` + 两工具 All checks passed）+ `py_compile` ×2 + `git diff --check` 全过 + **合成端到端 40/40 PASS**（fake-runner 四场景全 ok/monitor 失败/history 失败/insights 失败——退出码 0/1/1/1、skip 原因固定词汇、报告无绝对路径无子进程原文；真实只读 `monitoring_insights` 于 history canonical 目录形态（history.jsonl+history-summary.md 同目录）execute EXIT 0、输入逐字节零改动；`--source` 注入被白名单拒绝——全程临时目录，零生产管道执行）。**诚实边界：本切片开发回合零生产执行、零 scheduler mutation、零 canonical `.verify` 触碰；管道真实三步运行与计划任务下一轮观察由 supervisor 合并后在获准窗口进行（**已由 M14-22 于 2026-09-13 验收闭环**）；`production_ready=false` 不变**。详见 PROJECT_STATUS M14-21 条目与 `docs/evidence/m14-21-monitoring-insights-pipeline/README.md`）
- [x] M14-22 生产监控三步管道验收回填（✅ 2026-09-13 docs-only，零代码/零生产触碰——分支 `docs/m14-22-monitoring-pipeline-production-acceptance` 基于 `origin/main@1b89d91`（PR #98 merge），本 Claude 开发回合独占 worktree 仅一个本地 commit（含 supervisor R1 修正 amend）。Codex supervisor 真实生产验收事实回填入库：①PR #98 合并后 main CI run `34737912550` completed/success **5/5 job**（Web/Android/Release tools/Docker/API）；②计划任务 `AIOS-Monitoring-Pipeline` canonical 工具 status=**installed** 且 Action/参数/cwd/Hidden/触发器/间隔/时限逐项匹配，**两轮连续真实调度成功**——Last Run **12:30:01 与 12:45:01（+08:00）均 Last Result=0**、下一轮 13:00；③canonical 管道报告两轮（`pipeline-20260913-043007` 12:30 + `pipeline-20260913-044503` 12:45）overall_status 均 ok、**monitor→history→insights 三步全 ok exit 0**（第一轮 1.756/3.055/0.432s、第二轮 1.27/0.13/0.143s）、lock acquired/released=true，**insights 产物首次由真实计划任务产出（12:30）并在第二轮（12:45）刷新**（首轮 insights.json `d45c0bcc…b17b`/6112B、insights-summary.md `89e438d1…75b9`/2740B；第二轮 monitor-20260913-044501.json `6006ce2d…1d1d`/14346B、insights.json `8c82612d…1cea`/6277B、insights-summary.md `ac661d11…179c6`/2780B——两轮回填回合独立重算逐项一致；gitignored 不入库，仅安全摘要）；④monitor 工件两轮结论同构（12:30/12:45）：overall_status=warn/partial=false——**6/6 服务 healthy/running、5/5 端点 200 延迟全正常**，唯一 warn = api 容器 restart_count=1 达 restart_warn=1（两轮同一静态累计值；**静态累计阈值语义，非服务故障**），monitoring_ready=false；⑤insights-summary（12:45 刷新后）：**18 样本 ok=1/warn=17/critical=0、当前 warn 17 连**（最新样本 04:45:01Z 服务全 healthy/端点全 200、唯一阈值告警 restart_count=1）。**M14-21 遗留边界「管道真实三步运行与下一轮观察」闭环为已验收——两轮成功证明重复调度执行，不构成长期稳定性证明；仍未完成：外部告警接入、指标时序存储/查询、阈值随时间标定、真实客户端验收、>60s/真实负载/跨机长稳、AGC 发布、更长时间窗口的调度连续性观察；`production_ready=false` 不变**；证据 `docs/evidence/m14-22-monitoring-pipeline-production-acceptance/`）
- [x] M14-23 监控 restart 增量语义修复（✅ 2026-09-13 契约修复（含 supervisor R1 加固 amend）**+ 2026-09-13 真实计划任务验收闭环**——分支 `feat/m14-23-restart-delta-monitoring` 基于 `main@73d0443`（PR #99 merge），**已随 PR #100 合并 main**（merge `6efcdfdf46ff1638e41ee59f5329de544cdeb858`、feature head `5926172`（含 R1：milestone/canonical started_at_utc 基线身份 + symlink 防御 + history/insights baseline 元数据一致性），本地 git 可验证；PR CI run `34750025828` 与合并后 main CI run `34750207530` 均 5/5 job SUCCESS——supervisor 验收事实）。**真实调度验收（回填 2026-09-13）**：合并后 `AIOS-Monitoring-Pipeline` 自然调度两轮（18:00/18:15（+08:00）均 Last Result=0、monitor→history→insights 三步全 ok、零手动触发；durations monitor 4.068/4.947s、history 0.364/0.31s、insights 0.282/0.201s，lock true/true）——17:45 旧语义轮 overall warn（api 累计 restart_count=1、无 restart_evaluation）；18:00 新代码轮 overall **ok**/monitoring_ready true（同累计值、同容器实例 → state ok/reason stable/**delta 0**，基线取自 17:45 旧 v1 工件——向后兼容基线路径生产实证）；18:15 同构保持（滚动基线）；18:15 刷新后 history **40 样本 ok=3/warn=37**（最新行 ok，api 累计 1 与增量评估同档并存）、insights 当前 ok 连胜 ×2、**最长 non-ok 37 恰终止于旧语义最后一轮（09:45:02Z）**、恢复转移恰 1 次 @ 10:00:02Z、api 增量/事件/恢复计数全 0——**37 连 warn 首轮按设计恢复 ok 并保持**。修法要点：monitor 阈值改评**当轮新增增量**（当前累计 − 基线累计；基线 = 工件目录最新合法 prior 完整 monitor 工件，纯本地只读 fail-safe，零 shell=True/零网络/零额外生产读取，非法候选显式计数绝不静默当零基线，partial 恒布尔 False）；同实例增量 0 → ok，增量含边界 ≥1/≥5 判 warn/critical，started_at 变化=重建/累计下降=重置各一轮可见 warn（负增量绝不静默归零）后恢复，无基线 count>0 一次性 baseline-missing 告警后恢复；累计事实照实入档；报告加法字段 `threshold_results.restart_evaluation`，check_id 仍 `container-restarts`，v1 旧工件照常作基线；history 入档该可选字段（缺省=旧工件合法、在场即严格校验 fail-closed）+ 摘要 restart_delta_totals/restart_event_totals/restart_delta_sample_count（累计 restart_totals 口径不重定义）；insights 行级可选字段（旧行照常可读）+ service_summary 区分累计 restart_total 与当轮增量/事件/恢复计数 + Markdown 分列口径注记。TDD RED 先行；开发回合验证 = 监控家族五套件 **618 passed** + 全量 pytest（34 failed 与 main 既有失败同域）+ ruff + `git diff --check`（详见 PROJECT_STATUS M14-23 条目）。**诚实边界：两轮自然调度验证恢复语义正确性与短期（两轮窗口）稳定性——不构成外部告警、指标时序存储、长期稳定性、跨机监控的验证，更不构成 production readiness 宣称；重建/重置/baseline-missing 路径的真实生产触发尚未发生（仅契约测试覆盖）；`production_ready=false` 不变**。详见 PROJECT_STATUS M14-23 条目与 `docs/evidence/m14-23-restart-delta-production-acceptance/`）
- [x] M14-24 语音健康端点延迟/劣化生产阻塞修复（✅ 2026-09-13 契约修复，**已随 PR #102 合并 main + 受控生产验收闭环（回填 2026-09-14）**——merge commit `165ca5caff9b48e8514a741e77fe3420b12b5fee`、feature head `91da769`（本地 git 可验证）；PR CI 5/5 job SUCCESS、合并后 main CI run `34766519950` 5/5 job SUCCESS（supervisor 验收事实）；验收：受控重启后 PID 19132 于 2026-09-13T23:51:10+08:00 运行合并后代码（端口 8011、manifest managed-running）、真实 TTS 冒烟 18467ms/405164B/SHA-256 `C54E871D6FD12022085713D2A52DD3ED0483FF4C3A30A643D23958EC42E33DE3`、合成负载 28 并发探针零非 200（`/health` max 9.708ms、`/health/live` max 409.981ms）、自然监控 00:00/00:15 两轮 ok（各 34/0/0，CosyVoice 12.417/13.243ms——两轮自然观察不构成长期稳定性证明）；重启过程暴露 M14-25 阻塞点（模型缓存完整仍强制 pip 联网，PID 18447 离线 bootstrap 中止）——分支 `fix/m14-24-voice-health-latency` 基于 `main@c4bda69`（PR #101 merge），本 Claude 开发回合独占 worktree 仅一个本地 commit。生产触发：自然监控 14:00/14:45 轮 funasr-health 3619/4813ms + cosyvoice-health 1001ms（warn）、14:15/14:30 轮双 voice 端点 5s 超时（incomplete），容器面全程健康。根因（代码级）：①funasr（上游 `funasr==1.4.15`）转写与健康同跑单 uvicorn 事件循环、CPU 推理内联阻塞——第三方包不修改，**未修、记残余风险**（后续建议：跟踪上游或 8010 前轻量 health facade，需 supervisor 授权）；②cosyvoice bridge（本仓）合成在推理锁内整张量单次 `tolist` 非抢占独占 GIL 饿死健康线程 + 缺轻量 liveness。修法（`tools/voice/cosyvoice_openai_bridge.py`）：新增 `/health/live` 轻量 liveness（恒 200、零锁零模型工作、readiness 如实透出）；`/health` 既有 503/200 契约逐字节不变；`_tensor_to_samples` + `SAMPLE_CHUNK_SIZE=50_000` 分块转换 + 推理锁收窄到模型前向 + 转换移出锁外 + 块间 yield 释放 GIL（≤0 块大小 fail-closed）。新增 `test_voice_health_bridge.py` 14 项（liveness/冷启动/向后兼容逐字节/持锁并发探测/8 路并发/分块与 yield 边界/守卫/锚点；TDD RED 11 项实证后 GREEN）。验证 = 聚焦 14 passed + 邻居 `test_voice_local_scripts.py` 40 passed/1 skipped（合计 54）+ ruff + `py_compile` + `git diff --check`；零生产触碰、监控阈值零改动不遮蔽告警、合成/fake 测试、`production_ready=false` 不变。详见 PROJECT_STATUS M14-24 条目、`docs/evidence/m14-24-voice-health-latency/README.md` 与生产验收证据 `docs/evidence/m14-24-production-acceptance/README.md`）
- [x] M14-25 CosyVoice bootstrap 离线重启幂等修复（✅ 2026-09-14 开发完成，**已随 PR #104 合并 main + 受控生产验收闭环（回填 2026-09-14）**：merge commit `d54ad5b51ec7653c592786399f37a778f8c07e5d`、feature head `98a2d773c8e098e4a2ebf4328f82ccdcc25023b6`（本地 git 可验证），PR CI 5/5 job SUCCESS、合并后 main CI run `34803270943` rerun 后 5/5 job SUCCESS（首败为 Docker Hub redis 镜像拉取连接重置，外部基础设施侧；CI 为 supervisor 验收事实）；验收——重启前基线 CosyVoice PID 19132/FunASR PID 867 双健康（/health 各 200）、六容器 healthy；第一次 stop 因 WSL `/proc` 探测瞬时不可用 **fail-closed 拒绝且未动任何进程**（stop rc=3、未启动新实例）；默认环境重试 PID 19132 优雅退出、新 PID 26008 于 2026-09-14T11:52:45+08:00 启动；重启偏移日志硬证据：offline fast path 命中（pip check + CUDA closure + import + torchaudio WAV 探针全过、零网络安装命令）、模型 payload 跳过下载、wetext local-only/zero ModelScope traffic、禁用模式 grep `FORBIDDEN_INSTALL_DOWNLOAD_PATTERN_MATCHES=0`（无 pip install/upgrade、无 download.pytorch.org、无 Cloning into、无 FunAudioLLM/CosyVoice.git、无模型下载）；`/health` 与 `/health/live` 稳态 200 ready/alive；主 API 同款 provider TTS 成功 `local-cosyvoice` 21896ms/220844B/RIFF-WAV/SHA-256 `E78CC2FE0AB4D894160033F1B6975F9B802275CCD0ADE990EDAE34C688AA4ABD`（此前 smoke TTS 241964B/RIFF-WAV 亦成功，ASR health 因 WSL 转发层超时未通过）；12:00–13:30 共 7 轮自然监控 pipeline overall **failed 同构如实在案**（每轮六容器 compose ok、Web root/login 与 API health 200、仅 funasr/cosyvoice 两 Windows→WSL loopback 端点 5s TimeoutError；monitor incomplete/partial、history/insights skipped；CosyVoice 进程持续存活、WSL 内部探针曾 200——非引擎本体死亡；13:45 轮工件仍同签名）——**验收范围仅 offline fast path 重启幂等，不宣称 WSL localhost 转发长期稳定、不宣称全绿**。开发切片：分支 `fix/m14-25-cosyvoice-offline-restart` 基于 `origin/main@1f58800`（PR #103 merge，本地 git 可验证；开发中途按 supervisor R1 指令 rebase 自 `165ca5c`，与 M14-24 回填 docs 零冲突），本 Claude 开发回合独占 worktree 仅一个本地 commit。动机：M14-24 受控重启实证 PID 18447 在模型/venv/wetext 缓存完整时仍因脚本无条件 pip upgrade/install 与 CUDA closure 恢复联网而中止（PyPI 不可达）；最终 PID 19132 只是网络恢复后成功、非幂等修复。修法 = **offline fast path**：四道门禁（pip check / CUDA closure 运行期一致性 / import cosyvoice.cli.cosyvoice / torchaudio WAV 探针）原样函数化为单一事实源（`gate_pip_check`/`gate_consistency_probe`/`gate_import_probe`/`gate_wav_probe`，CUDA closure 契约表 18 项不动），venv 就绪后先以四道门禁判定——当且仅当全部通过即跳过整个依赖安装段（五类联网安装命令零执行）直达模型检查/启动；任一失败点名缺失项进既有安装路径、安装后门禁照常 fail-closed；判定只依据本地解释器/本地 metadata/本地探针，绝不先联网探测可用性；模型 payload/缓存回落/SKIP_DOWNLOAD fail-closed/wetext/bridge 段原样不动。测试 = TDD RED→GREEN 扩展 `test_voice_local_scripts.py` 5 项（文本契约 + fake venv 行为实证 hit/miss：hit 全程零 install/upgrade/index-url 只有只读 check/freeze、miss 走安装路径 + 门禁 fail-closed exit 1；含 R1 返工——初版 fake 布局少拼 `cosyvoice/` 层曾误触真实 clone，本轮数据对齐脚本拼接路径并加运行前防错位 assert）；验证 = `test_voice_local_scripts.py` 43 passed/1 skipped + `test_voice_service_control.py` 64 passed（voice_service_control.py 零改动）+ bash -n + py_compile + ruff + `git diff --check` 全过。**边界：开发验证是本地/契约验证（fake venv/payload/git）——真实离线生产重启已由 2026-09-14 受控生产验收闭环（见上）；`production_ready=false` 不变（验收范围仅 offline fast path 重启幂等；WSL loopback 转发层新生产阻塞见下方「M14 下一生产阻塞点」条目）**。详见 PROJECT_STATUS M14-25 条目与 `docs/evidence/m14-25-cosyvoice-offline-restart/README.md`（§8 生产验收回填））
- [x] M14-27 监控语音健康来源切换契约（基于 PR #106 merge `ec60a093`——monitor 新增 sidecar 来源：canonical manifest 严格校验 + RFC1918 bind +固定 18010/18011 `/health` + 64KiB 双检 + fail-closed 零回退；pipeline monitor argv 固定 sidecar；M14-27 测试 35 passed、聚焦 332 passed、监控家族六套件最终回归 635 passed。该里程碑仅为代码/契约切换，真实 sidecar 启动、生产监控切换与观察留 M14-28，`production_ready=false` 不变）
- [x] M14-29 sidecar status 路径契约修复（✅ 2026-09-15 已随 **PR #108 合并 main** 并经 M14-28 merge-commit 复验收口——feature head `527c454`、merge commit `1b6d862`（本地 git 可验证），PR CI run `34933123493` 与合并后 main CI run `34946366049` 均 5/5 job SUCCESS；分支 `fix/m14-29-sidecar-status-path` 基于 `d19c296`（PR #107 merge），2 文件窄改。M14-28 受控生产验收**止步于步骤 4 sidecar 启动**：控制器曾把 artifacts **目录**当 `--status-file` 传入，sidecar fail-closed 守卫正确拒绝目录目标（`unsafe-status-target`）于监听前退出、start 20s 落档等待超时 rc 1。修复只改调用契约、绝不绕过守卫：`--status-file` 与 start/stop/status 全部 probe 调用恒传精确文件 `.verify/artifacts/m14-26-voice-health-sidecar/sidecar-status.json`（仓库内 repo-relative/仓库外绝对 posix），而 manifest.log 与控制日志提示路径仍锚定 artifacts 目录；信号语义/保护 PID/保护标记/sidecar 本体/引擎/relay/M14-27 monitor 零改动。`test_voice_health_sidecar.py` 113 passed（含 8 项新回归、既有 105 项零削弱）、M14-27 套件 35 passed、ruff/py_compile/`git diff --check` 全过。**真实 M14-28 验收已在合并后于 merge commit `1b6d862` 整体重跑闭环：2026-09-15 复跑 9/9 PASS**（sidecar 切换/生命周期/监控集成范围；窗口内语音引擎停机、两 voice 端点经 sidecar 502 如实透传，monitor 2 critical/exit 2、pipeline overall failed 为环境真实状态与 fail-closed 语义，非 M14-28/M14-29 失败——证据 `docs/evidence/m14-28-voice-health-production-cutover/README.md`）；`production_ready=false` 不变。证据 `docs/evidence/m14-29-sidecar-status-path/README.md`）
- [x] M14-31 语音引擎受控恢复（FunASR/CosyVoice start + sidecar 状态回升 + 单轮只读监控全绿）（✅ 2026-09-15 22:56–23:08 +08:00 于 main@`4d6e5c8` 执行并经 **Codex 独立验收 PASS（10/10，本任务验收口径）**——全程 tracked clean、零代码改动、`.verify` 证据 gitignored 不入库。基线：双引擎 stopped（8010/8011 无监听）、sidecar PID 701 running-degraded [502,502]、compose 六服务 healthy。恢复：仅经 `tools/voice/voice_service_control.py start` 受控启动——FunASR PID 3445/8010 /health 200（sensevoice，模型自本地 modelscope-cache 加载无重下载）**78s** 达健康；CosyVoice PID 3851/8011 /health 200（Fun-CosyVoice3-0.5B-2512）**279s** 达健康（中间 `managed-running` + health 503 为工具文档明示的启动期相位，终态 200）；sidecar PID 701 未重启/未停止，18010/18011 `/health` 200、18011 `/health/live` 200（alive+ready），state `running-degraded` → `running-healthy`。单次只读 pipeline 23:05:22–24 **rc=0** 三步 ok：**34/0/0**、alerts=[]、partial=false、funasr-health 200（13.4ms）、cosyvoice-health 200（3.2ms）；工件哈希链 monitor `81d6c283…`/history `9c1f2ee4…`/insights `3412dfe9…` 互相引用一致；secret 扫描 9 类 0 命中。如实披露：restart_evaluation 基线引用的 23:00:01 monitor 轮疑为周期性监控、非本任务动作。诚实边界：单轮恢复 + 单轮监控全绿不证明长期稳定性、不构成全局 `production_ready=true` 依据；本轮 monitor 报告 `monitoring_ready=true` 为单轮逐轮字段值（≠ production ready）。证据 `docs/evidence/m14-31-voice-engine-recovery/README.md`（M14-32 回填））
- [x] M14-33 真实本地语音 ASR/TTS 端到端冒烟验收（恢复后真实 adapter 链路，一次性执行）（✅ 2026-09-15 23:51:34–23:52:05 +08:00 于 main@`7eb7404`（PR #110 merge，本地 git 可验证；任务前后同一 HEAD、porcelain=0、tracked 全程 clean）一次性执行仓库既有 `tools/voice/smoke_local_voice.py`（主 API 同款 adapter 链路——`LocalFunAsrAsrProvider`/`LocalCosyVoiceTtsProvider`、真实 HTTP、无 mock、既有样例未下载）并经 **Codex 独立验收亦 PASS**——**PASS（5/5，本任务验收口径）**：rc=0、stderr 空、stdout 末行 `ALL LOCAL VOICE SMOKE CHECKS PASSED`；ASR provider=`local-funasr` latency 2900ms 非空真实中文转写 `'希望你以后能够做的比我还好哟'`（confidence=0.000 如实记录——bridge 不返回置信度，判据是非空真实转写而非置信度）；TTS provider=`local-cosyvoice` latency 27066ms（CPU 本轮观测值）241964 bytes RIFF/WAV yes；样例 `artifacts/voice/smoke/asr_sample_zh.wav` 334138 bytes SHA256 `c7b31d6d…c8ff6` 前后一致未下载；FunASR PID 3445（22:56:56）/CosyVoice PID 3851（22:59:24）/sidecar PID 701（16:50:38）前后不变且 started_at 不变；direct 8010/8011 与 sidecar 实际 bind `172.25.7.64` 的 18010/18011 `/health`、18011 `/health/live` 前后均 200（5/5）；前置自然监控连胜 4 轮 ok（23:05/23:15/23:30/23:45 均 34/0/0——非 M14-33 触发、未执行 monitoring_pipeline/未修改监控工件）。诚实边界：单次、单样例、单文本真实 adapter 冒烟——不覆盖真实客户端/LiveKit 会话、移动端录音播放、长稳（soak）、并发负载、真实客户端负载形态；TTS 27s 为 CPU 单轮观测值；全局 `production_ready=false` 不变。证据 `docs/evidence/m14-33-real-voice-e2e-smoke/README.md`（M14-34 回填；原始证据 gitignored `.verify/m14-33-real-voice-e2e-smoke/` 不入库））
- [x] M14-35 Web 真实 LiveKit 客户端连接验收第一切片（真实浏览器 + 当前部署栈）（✅ 2026-09-16 开发完成并于 Supervisor Review Round 1 八项返工 amend（唯一随机房间防并发互听、rounded-lg、CI vitest 门禁、proxy env 校验 fail-closed、console 分窗硬断言、首页准确文案、npm audit 0 漏洞、证据重跑同步）于分支 `feat/m14-35-web-livekit-client`（基于 `main@a109c973`（PR #111 merge，本地 git 可验证），本 Claude 开发回合独占 worktree 单 local commit，remote 发布/CI/合并由 Codex 负责——开发时点状态）。补上 M14-33 明示未覆盖的「真实浏览器客户端 + LiveKit」链路第一切片：apps/web voice 首页新增克制「LiveKit 连接检测」卡片（`livekit-client` `Room.connect` 直连当前部署 LiveKit + 数据通道 `publishData` + 权限可用时发布真实本地麦克风音轨 + 恒跑退出清理；token 只经局部变量喂 SDK 绝不进 state/DOM/日志）+ `livekit-check.ts` 检测状态机（五步/outcome 分级/麦克风降级分类/JWT 脱敏/超时；R1 唯一随机房间生成器——Web Crypto+拒绝采样，每次检测 `web-check-<16 位随机字符>`，消除固定房间并发互听）22 项 vitest TDD + `api.voiceToken` 对齐既有后端契约（零 API 改动）+ `next.config.ts` env 门控 `/api/*` 同源 rewrite（R1：`AIOS_ACCEPTANCE_API_PROXY` 经 `acceptance-proxy.ts` 结构化校验——仅 http/https、去尾斜杠、拒查询串，非法值构建期 fail-closed；+7 项 vitest；默认构建零变化——routes-manifest rewrites 为空实测）；`infra/verify_web_livekit_client.py` 可重复真实浏览器验收工具（复用生产 API/LiveKit/DB 零重启，本分支构建空闲端口自起自收，Playwright Chromium + fake 麦克风，DOM 级断言 + JWT 扫描，fail-closed 0/1/2）。R1 其余返工：检测卡片 rounded-lg、CI Web job 增 `npm run test --workspace apps/web` 门禁（job 更名 Web (test / typecheck / lint / build)）、首页文案改为准确状态。真实浏览器验收（R2 修订判定：R1 后重跑 13/13 为历史记录；R1 首跑 connect 失败并非「环境抖动」，根因为默认拓扑间歇性 ICE 失败——当前 LiveKit 以 --node-ip 127.0.0.1 通告媒体且 UDP 仅绑 loopback，Chromium/WebRTC 默认不收集 loopback ICE candidate，Codex 独立默认拓扑首跑 connect 失败 `could not establish pc connection`、同构建复跑 12/12——拓扑级不确定）：验收脚本 R2 起 Chromium 显式加 `--allow-loopback-in-peer-connection` 受控条件（生产用户浏览器默认不具备），gitignored `.verify/m14-35-web-livekit-client-r2/` 受控连跑 run1/run2 通过、run3 connect 步失败（受控 flag 未完全消除间歇性）、run4/5/6 复跑连续三次通过——**末段连续 3/3 为受控拓扑验收通过，非默认浏览器直连稳定性**；受控通过段五步 token/connect/data/mic/cleanup 全 passed（fake 麦克风音轨真实发布到生产 LiveKit，房间为运行时唯一随机名）、`ws_url=ws://127.0.0.1:7880`、DOM 不含 JWT 形态 token、检测窗口 console/pageerror 硬断言为 0（登录窗口预期 401 单独记录不计入）。本地验证：单测 RED→GREEN 29/29（22+7）、typecheck 0 error、lint 0 errors（11 warnings 全为既有文件）、build 成功（9 路由；默认 env 下 rewrites 为空）、`npm audit --omit=dev`（官方 registry）0 vulnerabilities、API 合约未改 pytest 不适用。诚实边界：**连接诊断切片**——只验证「浏览器到 LiveKit 的连接与麦克风发布」，不代表完整语音会话（ASR/TTS）已接入；单次单浏览器（唯一随机房间）不覆盖远端订阅/多人房间/重连恢复/长稳/并发；默认拓扑间歇性 ICE 失败未消除（受控 flag 下 run3 仍失败），默认拓扑稳定化（LAN/TURN/显式绑定设计）为独立生产阻塞项；不构成 `production_ready=true` 依据，全局 `production_ready=false` 不变。证据 `docs/evidence/m14-35-web-livekit-client/README.md`（原始证据 gitignored `.verify/m14-35-web-livekit-client/`（R1）与 `.verify/m14-35-web-livekit-client-r2/`（R2 受控连跑）不入库））
- [x] M14-36 Web 验收工具进程生命周期修复（Windows 孤儿进程树精确回收）（✅ 2026-09-16——分支 `fix/m14-36-acceptance-process-cleanup` 基于 `main@3a5c095`，本 Claude 开发回合独占 worktree 单 local commit 不 push。缺陷：`infra/verify_web_livekit_client.py` 旧版以 npm 包装链为 Popen 对象、结束时只 terminate 包装器单 PID，Windows 进程终止不级联子进程 → 跨次验收累计 26 个 node.exe/mise.exe `next start` 孤儿进程锁死 worktree（监督者盘点实证，开工前已清理）。修法：`node -p process.execPath` 解析真实 node 直启 `node_modules/next/dist/bin/next start -p <port>`（workspaces 根优先、惰性 fail-closed），Popen PID 即最终进程本身；`stop_process_tree` 有界精确树回收——Windows 恰 `taskkill /PID <Popen pid> /T /F`、POSIX 独立进程组 SIGTERM→SIGKILL，绝不按端口/宽进程名扫杀。TDD 20 项全 mock 契约测试（含文本锚点与禁词）。验证：聚焦 20 passed + 全量 services/api 2826 passed/0 failed/33 skipped + ruff/py_compile/`git diff --check`；真实受控浏览器验收一次 13/13 verdict=passed（生产栈零重启），**验收前后孤儿扫描（node/mise/cmd 命令行含本 worktree 路径）均 FOUND 0——旧形态退出必残留、修复后退出即零残留**。边界：POSIX 组杀仅 mock 契约验证；不构成 `production_ready=true` 依据。证据 `docs/evidence/m14-36-acceptance-process-cleanup/README.md`）
- [x] M14-37 LiveKit 默认拓扑稳定性（默认/受控浏览器模式开关 + 媒体面独立绑定）（✅ 2026-09-16——分支 `fix/m14-37-livekit-default-topology` 基于 `main@54e8005`（PR #113 merge），本 Claude 开发回合独占 worktree 单 local commit 不 push。M14-35 遗留生产阻塞：默认浏览器直连本机 loopback LiveKit 间歇性 ICE 失败（Chromium 默认不收集 loopback candidate）。修法双侧：① `infra/verify_web_livekit_client.py` 新 `AIOS_LIVEKIT_BROWSER_LOOPBACK` 严格开关——未设置/0 = default 模式**绝不注入 loopback flag**（真实生产拓扑口径，失败即如实计数）；1 = controlled 受控模式（M14-35 R2 口径保留）；非法值 10 变体 ENV-BLOCKED fail-closed 退出码 2；flag 字面量收敛 `LOOPBACK_FLAG` 常量唯一定义处（契约测试锁定源码恰好出现一次）；results.json browser 段记录 mode/flag present/argv 摘要（无凭据）。② `AIOS_LIVEKIT_BIND_IP` 媒体面独立绑定——livekit 端口回落链 LIVEKIT_BIND → BIND → 127.0.0.1（未设置存量零变化）、node-ip 回落链 EXTERNAL → LIVEKIT_BIND → 127.0.0.1、`HOST_LIVEKIT_BIND_IP` 透传 settings、`validate_exposure` 新增 LiveKit-only 公开分支 fail-closed（强 LIVEKIT_API_SECRET ≥32 字节非公开默认占位 + 可达 PUBLIC_LIVEKIT_URL，API/Web 仍 loopback 时 APP_ENV/CORS 门禁不适用；API 面公开时 M9-06/M9-07 原语义零改动）；文档 `.env.example` + `docs/DEVELOPMENT.md`。TDD 新增 13 测试函数（开关语义/argv 纯净性/browser 段脱敏/源码契约/暴露校验纯函数/compose 渲染矩阵——livekit 走 LAN 而 api/web 仍 loopback）。验证：聚焦 76 passed + ruff/py_compile/`git diff --check`/增行 secret 扫描 0 凭据；真实默认浏览器验收 **8/8 连跑 verdict=passed**（每轮 mode=default、flag absent、五步全 passed、生产栈零重启零 env 变更）；受控回归 1/1 passed（旧口径不破坏）；非法值探针 ENV-BLOCKED/2；8 连跑后 worktree 孤儿扫描 FOUND 0（M14-36 不回归）。边界：8/8 为间歇性失败未复现实证而非 loopback 拓扑已稳定（生产栈未动，风险仍在）；LAN 绑定以能力形态交付，实际切换与切换后默认拓扑验收留运维显式执行；不覆盖远程设备/跨 NAT（M10-05 TURN 后备域）/多人/重连/长稳；不构成 `production_ready=true` 依据。证据 `docs/evidence/m14-37-livekit-default-topology/README.md`）
- [x] M14-38 LiveKit LAN cutover 生产收口（执行事实回填 + 工具固化）（✅ 2026-09-16——分支 `ops/m14-38-livekit-lan-cutover` 基于 `main@902dca2`（PR #114 merge），本 Claude 开发回合独占 worktree 单 local commit 不 push。**生产执行事实（supervisor 执行，本回合零生产操作仅记录）**：生产彩排栈 LiveKit 信令/媒体面已切本机 LAN IP `192.168.8.3`——`infra/env.production-recovery`（gitignored）原子更新 `AIOS_LIVEKIT_BIND_IP=192.168.8.3` + `AIOS_PUBLIC_LIVEKIT_URL=ws://192.168.8.3:7880`（`AIOS_BIND_IP` 未动仍 `127.0.0.1`——信令/媒体面独立绑定即 M14-37 交付的能力）；切换前备份 `artifacts/ops/livekit-cutover/20260916-122542-env.backup`；最小范围重建仅 api（`b3e62b355703`）/livekit（`b21a1802e4ae`）两容器，web/redis/postgres/minio 未触碰，六容器 healthy；API 实测 `ws_url=ws://192.168.8.3:7880`；**真实默认浏览器 3/3 通过**（`AIOS_LIVEKIT_BROWSER_LOOPBACK` default 模式绝不注入 loopback flag——M14-35 遗留 loopback 媒体面间歇性 ICE 失败随之收口），证据 gitignored `.verify/m14-38-production-lan-default-run1..3`；切换后只读监控 `overall_status=ok` 34 ok/0 warn/0 critical（证据 `.verify/m14-38-production-monitor-post-cutover`）。**代码收口（worktree 交付，开发验证为 FakeRunner 契约测试；cutover 工具交付后经 supervisor 两阶段真实 Docker 验收——首轮 apply 缺 --no-deps 暴露依赖隔离缺陷（compose 连带重建 minio 因 aios/minio 镜像缺失失败，六容器原样 healthy；env 采纳第九键 AIOS_BIND_IP 后 plan 9/9），修复补 --no-deps 后 20260916-181457 apply 成功——api 与 web/redis/postgres/minio 容器 ID 不变、livekit 重建 `49935f127ca0` healthy、复核 pin 9/9，默认浏览器验收 `.verify/m14-38-production-lan-default-r4-post-nodeps` 通过）**：① `infra/verify_web_livekit_client.py` LiveKit 信令探测去硬编码——旧版 preflight 硬编码探测 `http://127.0.0.1:7880` 在 LAN 拓扑下必误报，现改「契约派生优先 + 显式 override 严格校验」：默认由登录后 `POST /api/v1/voice/token` 响应 ws_url 派生同源健康 URL（与浏览器实际地址同源，不重造第二事实源；loopback/LAN 两类拓扑零改动覆盖），`AIOS_PUBLIC_LIVEKIT_URL` override 仅 ws/wss + 非空主机名、空串/非法 ENV-BLOCKED fail-closed，契约测试 72/72；② 三拓扑键纳入 production recovery pin（`PIN_KEYS` 六→九键：`AIOS_BIND_IP`/`AIOS_LIVEKIT_BIND_IP`/`AIOS_PUBLIC_LIVEKIT_URL`——compose 缺省回落链会把 LAN 拓扑静默重建回 loopback，不 pin 即防漂移缺口；在线事实取 api 容器 env 与 `docker port livekit 7880` 宿主绑定段，空串也是事实；输出恒仅键名），`.example` 模板同步九键，契约测试 53/53；③ 新工具 `tools/ops/livekit_lan_cutover.py`（533 行）+ 契约测试 29/29——把 supervisor 手工切换固化为可重复 runbook：plan 只读 delta 预览（main 级恒零文件系统写）/ apply（gate→备份+sha256→原子更新仅拓扑键→compose config 校验失败即恢复零重建→up -d --no-build --no-deps 仅 api livekit（--no-deps 隔断 compose 依赖解析，绝不连带重建 depends_on 依赖）→九键复核必须全绿）/ rollback（伴生 sha256 校验先行——-env.backup→-env.sha256 推导，缺失/格式非法/文件名不匹配/哈希不匹配任一即拒绝且零副作用：不读 pin 值/不建 safety 备份/不调 Docker/不写 env→备份九键校验→回滚前快照→原子恢复→同样最小范围重建+复核）；执行门 `--confirm-rebuild rebuild-api-livekit` 精确值；源码契约锁定绝不 stop/rm/kill/down/restart/reset/--build、绝不触碰 web/redis/postgres/minio、secret 永不落日志。边界：`production_recovery --dry-run` 仍被 MinIO 缺镜像阻塞（M14-23 既有，未被本次改变）；`production_ready=false` 不变；远程设备/跨 NAT 仍属 M10-05 TURN 后备域。证据 `docs/evidence/m14-38-livekit-lan-cutover/README.md`）
- [x] M14-39 production_recovery 健康栈跳过 up + up 前本地镜像预检（恢复边界固化）（✅ 2026-09-16——分支 `fix/m14-39-recovery-minio-preflight` 基于 `main@7f1fe91`（PR #115 merge），本 Claude 开发回合独占 worktree 单 local commit 不 push。M14-38 记录在案的诚实边界「`production_recovery --dry-run` 仍被 MinIO 缺镜像阻塞」收口：生产栈六容器均 healthy，而本机缺 M14-13 自建镜像 `aios/minio:RELEASE.2025-10-15T17-29-55Z`（registry 不可拉取）——旧 dry-run 语义 pin 9/9 后无条件 `up -d --no-build --dry-run` 计划 Recreate api/livekit/minio 因缺镜像 rc=1，把「栈已健康」误报为恢复失败；手工验证 no-recreate+pull never 可 rc=0 但隐藏计划重建，不采纳。修法：① pin 通过后 up 前只读 stack health 快照——六服务全 healthy/running 时 dry-run 与 enforce **一致跳过 compose up**（输出「健康栈无需 up」，继续语音调和与最终判定；startup recovery 恢复健康、不做部署/config drift 收敛，拓扑变更由 M14-38 工具显式执行）；② 非全健康保留原唯一 up 语义 `up -d --no-build`（dry-run 加原生 `--dry-run`）——不加 `--no-deps`/`--no-recreate`、不隐藏漂移（与 M14-38 cutover 最小范围重建分工）；③ up 前只读本地镜像预检（仅 up 路径）：`LOCAL_BUILD_IMAGE_REFS`（与 compose `image:` 锚点契约测试交叉锁定）逐个 `docker image inspect`（零 pull/build/stop/restart）——缺失且栈非全健康 → 固定词汇 `minio-local-image-missing` fail-closed（enforce 立即退 1、dry-run 退出码镜像 enforce 拒绝，绝不 pull/build）；④ 健康栈跳过 up 即不触发预检。PIN_KEYS 九键/pin 语义/单文件纯标准库/Runner 注入/M14-38 复用面零改动。TDD 先 RED 后 GREEN：test_production_recovery.py +10 项（双模式健康跳过、唯一 up 语义、MinIO 缺镜像 fail-closed 双模式、健康栈+缺镜像仍 OK、快照/预检只读、常量与 compose 锚点交叉锁定、gaps 纯函数、M14-38 不回归），源码契约禁词扩展。真实验证全程只读：主仓 venv dry-run **exit 0**（pin 9/9、六服务健康快照、零 up 构造、funasr/cosyvoice leave、最终 OK——旧语义同栈将 rc=1）；前后 `docker ps` 六容器 ID/时长/状态完全不变；只读实证本机确缺 aios/minio 镜像；聚焦 92 passed（63+29）、全量 services/api **2929 passed/0 failed/33 skipped**（3:42）、ruff/py_compile/`git diff --check`/secret 扫描全过。诚实边界：minio 自建镜像仍未本机构建/采纳（栈真实非全健康需重建 minio 时仍 fail-closed，人工构建+数据卷属主迁移另办）；fail-closed 真实 Docker 路径与 enforce 真实执行未在本机触发（契约测试双模式覆盖）；`production_ready=false` 不变。证据 `docs/evidence/m14-39-recovery-healthy-skip/README.md`）
- [x] M14-40 MinIO 本地镜像采纳预检工具（build/smoke/preflight 三模式）（✅ 2026-09-16——分支 `ops/m14-40-minio-local-adoption` 基于 `main@0e4f5c5`（PR #116 merge），本 Claude 开发回合独占 worktree 单 local commit 不 push，零 Docker/生产操作。M14-39 遗留「minio 自建镜像仍未本机构建/采纳」的接续第一切片：交付 `tools/ops/minio_image_adoption.py`（纯标准库）+ `services/api/tests/test_minio_image_adoption.py` 120 项契约测试。三模式各默认 plan 零执行，真实执行需 `--execute` + 模式专属精确确认短语（EXECUTE MINIO IMAGE BUILD / EXECUTE MINIO IMAGE SMOKE / EXECUTE MINIO PREFLIGHT）：build=恰好一次 docker build（只构建 compose 锚定 `aios/minio:RELEASE.2025-10-15T17-29-55Z`，pin commit 四处交叉锁定；可选 `AIOS_MINIO_BUILD_HTTPS_PROXY`（socks5h/socks5/http/https + host[:port]，端口 1–65535 语义校验）只传 Docker 预定义 HTTPS_PROXY build arg——零 GOPROXY/Dockerfile/pin 改动）；smoke=严格 `aios-m14-40-` 前缀一次性容器/卷 + loopback 19000/19001 + uid 1000 探针 + finally 精确清理恰两名（清理失败即整体失败）+ 绝不读 env secret；preflight=只读生产盘点（镜像元数据与运行时 uid/版本、compose/Dockerfile 锚点、六容器健康、生产卷 driver/size/递归 UID 普查——卷探针恒 `:ro` 挂 `--network none` 一次性 `--rm` helper，绝不写生产卷），采纳判定 fail-closed：镜像缺失/user/entrypoint/version 不符/卷属主非 uid 1000 或普查不确定/栈非全健康 → adoption=blocked。生产面恒只读（`--project` 仅 aios-m14-03-production-rehearsal；零 compose mutation/pull/stop/restart/remove/exec/env-secret-read）；每个 docker argv 固定结构白名单，偏离执行前拒绝；报告 schema v1 JSON+MD 落 gitignored `.verify/artifacts/m14-40-minio-image-adoption/`；退出码 0 成功/2 一切失败（fail-closed 统一）。真实构建尝试如实入档：2026-09-16T12:51:06Z 无代理直接构建 468.7s 于 Go module 拉取阶段失败（proxy.golang.org connection refused，docker build rc=1——gitignored build-20260916-125106.json/.md），代理重试路径已备；真实代理构建、smoke、preflight、push/PR/CI 待 supervisor 执行，不宣称通过。验证：全量 services/api 3045 passed/33 skipped、聚焦 120 passed、邻居复验 91 passed/3 skipped、ruff/py_compile/`git diff --check`/secret 扫描/端口穷尽检查全过。边界：adoption=pass ≠ 生产就绪（`production_ready=false` 不变）；不把自建镜像采纳进生产、零数据迁移——minio-data 卷 root→uid 1000 一次性迁移 + `up -d --no-build` 固化为后续受控任务。证据 `docs/evidence/m14-40-minio-local-image/README.md`）
- [x] M14-41 MinIO 生产数据卷属主采纳工具（root → uid 1000，plan/execute/rollback）（2026-09-17——M14-40 记录的「minio-data 卷 root→uid 1000 一次性迁移」接续切片：交付 `tools/ops/minio_volume_adoption.py`（纯标准库）+ `services/api/tests/test_minio_volume_adoption.py` 契约测试。三阶段各产 JSON+MD 报告：execute/rollback 需精确确认短语；chown 前强制 tar + SHA-256 + manifest 备份；rollback 先校验备份工件与镜像 ID（不符即 image-id-mismatch 拒绝）再停止/恢复；仅 MinIO 容器被停止/重建，其余五容器只读基线采集；一次性 helper 容器 `--user 0:0` + 最小化挂载，完成后销毁；停止容器前 compose/env 元数据门禁（--env-file + --profile local）；一次性 helper 与工件路径全部 fail-closed（含符号链接组件检查）；argv token 白名单（未知参数一律拒绝）。验证：卷采纳套件 106 + 镜像采纳套件 127 + Codex 合并 233 passed，ruff/py_compile/`git diff --check` 干净。开发全程零 Docker/零生产（mock 契约测试）。**生产采纳已闭环（2026-09-17）**：已随 PR #118 合并 main `5bc74c4`（合并前跨平台测试修正，PR CI run `35172658746` 五 job 全部通过；本地 Windows 卷+镜像组合测试 233 passed）；合并后 main 真实 plan exit 0；真实 execute stamp `20260917-020446` 于 2026-09-17T02:05:03Z 判定 pass——root 普查迁移 uid 1000、备份 `minio-data-backup-20260917-020446.tar`（57344 bytes，SHA-256 `c56298300087f1493f59c257f9f65206231aca10c82a4c7f35a6f3806f8a38b8`）tar/.sha256/.manifest/report 工件齐备、MinIO 以容器 `8e4f3d855ffb` 重建（本地 pin 镜像 `sha256:0f1c79afdb0b5fcdd49e385c46bca065f97cdd89917522593b84436a2e61bcd6`）healthy、API `b3e62b355703`/Web `5be2e19db2e7`/LiveKit `49935f127ca0`/Postgres `d17d5a93a079`/Redis `89f3ed0fd02a` 容器 ID 不变 healthy；采纳后 M14-40 preflight stamp `20260917-020606` 全部检查通过（采纳边界解除）；采纳后 `production_recovery --dry-run` exit OK（compose config OK、pin 9/9、六服务 healthy、健康栈跳过 up、FunASR/CosyVoice managed-running health 200 未触碰）。rollback 未在真实生产演练；execute pass 与 preflight pass 不等于 production_ready，`production_ready=false` 不变。证据 `docs/evidence/m14-41-minio-volume-adoption/README.md`）
- [x] M14-42 生产审计链初始锚定记录（docs-only 回填）（✅ 2026-09-17——分支 `docs/m14-42-audit-anchor-record` 基于 `origin/main@8172229`（PR #119 merge），本 Claude 开发回合独占 worktree 单 local commit 不 push，零代码/零测试/零生产操作——只把 supervisor 已执行的生产审计链初始锚定事实入库（工具为已在 main 的 M10-06 `services/api/app/ops/audit_chain_anchor.py`，Alembic `0027_audit_chain` 生产 current==head）。supervisor 事实链（UTC）：02:28:14Z audit-chain-anchor dry-run valid（DB 链 0 entries / 0 audit rows 全零 genesis，提案 sequence 0 创世锚、未写入）；02:28:50Z `--yes` 写入库外锚文件（生产侧 `/tmp/aios-audit-anchor.jsonl`）——anchor_hash `59c672b9be0ea82dddd0d01e2f899b217674a4796b4f91f2381900e0af627c02`（anchored_at `2026-09-17T02:28:50.131481+00:00`，schema_version 1 / sha256，head 与 previous 均为 genesis 全零常量）；02:29:48Z production-preflight post-migration 只读复核 5/5 pass、0 fail / 0 pending / 0 not_configured（exit 0，工件 generated_at `2026-09-17T02:30:16.065512+00:00`），治理三项计数均为 0（unowned_non_seed_papers / course_generation_null_owner_drafts / variant_question_null_owner_drafts）；写入后 verify-only 复核 up-to-date、problems=[]。入库证据 `docs/evidence/m14-42-audit-chain-anchor/audit-anchor.jsonl` 原样复制（354 bytes，SHA-256 `d2bfd877aa94632e6f68932a4d4bef8d963a6eb61aca12de6429c5fb7873aa4e`，仅 schema/algorithm/sequence/hash/time 字段无敏感值）+ 同目录 README（verify-only.json 与 production-preflight.json 留 gitignored 不入库）。诚实边界：本回合零生产操作，只记录 supervisor 已执行事实；tracked anchor + merge commit 只能作为 Git 库外见证 / 哈希存证，不等同严格 WORM / 对象锁 / 离线介质，专项归档仍开放；provider 冒烟与发布审批仍未完成；`production_ready=false` 不变。证据 `docs/evidence/m14-42-audit-chain-anchor/README.md`）
- [x] M14-43 审计锚点 WORM 归档工具（✅ 2026-09-17 开发切片——分支 `ops/m14-43-audit-worm-archive` 基于 `35c0875`（M14-42 docs commit），本 Claude 开发回合独占 worktree 单 local commit 不 push，零真实 S3/零生产操作。M14-42 记录的「锚文件严格 WORM 归档仍开放」接续切片：交付 `tools/ops/audit_anchor_archive.py`（单文件纯标准库）+ `services/api/tests/test_audit_anchor_archive.py`。三命令 `preflight` / `archive` / `verify`，退出码统一 0 成功 / 2 一切拒绝（fail-closed）：preflight 零写——本地锚链完整校验与 M14-42 生产端 `audit_chain_anchor` 同契约（sequence 非负严格递增允许跳号、anchored_at ISO-8601、anchor_hash 重算、genesis 常量、symlink 拒绝）+ endpoint 公网必须 HTTPS / loopback+RFC1918 可 HTTP / userinfo 内嵌凭据拒绝 + bucket 存在/versioning `Enabled`/Object Lock enabled；archive 三道参数门（确认短语 `EXECUTE AUDIT ANCHOR WORM ARCHIVE` 一字不差 + 仅 COMPLIANCE 不实现可绕过的 GOVERNANCE + tz-aware 严格未来 retain-until，缺一零执行零报告）、对象 key 内容寻址 `audit-anchor/<sha256>/audit-anchor.jsonl`（已存在字节不符 fail-closed、字节相符核验 retention 后绝不覆盖；新对象恰一次 put_object（x-ndjson + SHA-256 checksum），put 后重读字节与元数据任何漂移 fail-closed）；verify 按归档报告 version 定向 head/get + `.json.sha256` sidecar 哈希核验 + 报告绑定当前调用事实（跨 bucket/endpoint 或失败归档报告一律拒绝）+ 逐字节 SHA-256/version ID/COMPLIANCE/retain-until/content-type/size 精确核验。安全纪律：S3 凭据只认 `AIOS_AUDIT_ARCHIVE_ACCESS_KEY` / `AIOS_AUDIT_ARCHIVE_SECRET_KEY` 环境变量（绝不接受 CLI 值、绝不记录其值）；S3 API 白名单仅 read/head/put 六方法（head_bucket / get_bucket_versioning / get_object_lock_configuration / head_object / get_object / put_object），零 delete/copy/create-bucket/put-bucket-config（源码契约测试锁定），绝不删除、绝不覆盖任何已归档对象；报告原子写 gitignored `.verify/artifacts/m14-43-audit-worm-archive/`（字节模式写盘防 Windows 行尾翻译破坏 sidecar 哈希、endpoint 只记 host、redact_secrets 终防线）；S3Client/FS/Clock/env 全注入零网络，boto3 仅真实适配器工厂内懒导入、hex SHA-256 → base64 只在 boto3 边界转换。验证（全零网络）：TDD RED 39 failures 先行，supervisor R1 11 项修正全落实；聚焦 **151 passed** + 邻居 audit_chain_anchor/release_readiness **90 passed / 3 skipped** + ruff（含 RUF059/UP012/BLE001 显式复核）/py_compile/`git diff --check`/diff secret 扫描全过。**诚实边界：纯开发验证（注入 Fake 契约测试）——零真实 WORM 归档执行（M14-42 创世锚未被归档动作触碰、未连接任何真实 S3）；真实 preflight/archive/verify 由 supervisor 在获准窗口执行；离线介质第二副本与定期归档调度仍属运维动作；`pass` ≠ production ready，`production_ready=false` 不变**。证据 `docs/evidence/m14-43-audit-worm-archive/README.md`）
- [ ] M14 下一生产阻塞点（**M14-23 restart 增量语义已合并 main 并经真实调度两轮验收闭环（PR #100 merge `6efcdfd`；18:00/18:15 自然调度 37 连 warn 首轮恢复 ok 并保持）——监控语义面收口，剩余为接入与长稳面**；**M14-24 已修 cosyvoice 健康面并经受控生产验收闭环（PR #102 merge `165ca5c`：受控重启 PID 19132 运行合并后代码、真实 TTS 冒烟、合成负载 28 并发探针零非 200、自然监控 00:00/00:15 两轮 ok）——funasr-health 上游事件循环阻塞未修为已知残余风险（ASR 负载期仍可能 warn/incomplete，属预期非回归），后续授权后评估上游跟踪或 health facade；验收重启过程暴露的功能切片 M14-25（CosyVoice bootstrap 离线重启不幂等——模型缓存完整仍强制 pip 联网，PyPI 不可达时新进程无法启动，PID 18447 实证）**已随 PR #104 合并 main 并经受控生产验收闭环（merge `d54ad5b`：offline fast path 重启命中、零网络安装/零模型下载、稳态健康、主 API TTS 成功——12:00 自然监控 failed 如实记录，验收范围仅重启幂等）**。**新生产阻塞（2026-09-14 12:00–13:30 七轮自然监控同构实证、13:45 轮仍同签名）：Windows→WSL loopback/relay 稳定性——Windows 侧 8010/8011 listener 由 wslrelay.exe PID 17936 持有（listener 启动 2026-09-12 20:44:46，同一实例跨 M14-24/M14-25 两次故障窗口在场），`wsl.exe` 管理面间歇 `WSL/Service/0x8007274c`/`TimeoutExpired`；funasr/cosyvoice 两 loopback 端点持续 5s 超时致 pipeline overall failed、history/insights 连续 skipped（每轮六容器与 Web/API 正常、WSL 内部 FunASR 探针曾 200、CosyVoice PID 26008 持续存活、Windows 侧曾短暂恢复 200——非引擎本体死亡；非 M14-25 回归，M14-24 时点已有同类先例）→ M14-26 已交付建议②的健康路径旁路（WSL 内双端口只读窄代理 18010/18011 + Windows 侧受控生命周期控制器——已随 PR #106 合并 main，merge `ec60a093`）；M14-27 监控语音健康来源切换已随 PR #107 合并 main（merge `d19c296`）；**M14-28 受控生产验收已在 PR #108 merge `1b6d862` 上整体复跑 9/9 PASS（2026-09-15：受控 start argv 恒传精确 status 文件、PID 703/复启 701、bind 172.25.7.64、18010/18011 单属主 listener、502 如实透传、幂等、精确回收 stop、伪造 PID 867 fail-closed 拒绝零信号、真实只读监控管道以 `voice_health_source=sidecar` 从 canonical manifest 派生端点完成完整采集——M14-26→28 链（sidecar→manifest→monitor）端到端 live 验证，③ 监控端点切换到 sidecar 旁路已收口；窗口内 FunASR/CosyVoice 停机、两 voice 端点 502 如实致 monitor 2 critical/exit 2、pipeline overall failed——环境真实状态与 fail-closed 语义，非 M14-28/M14-29 失败；证据 `docs/evidence/m14-28-voice-health-production-cutover/README.md`）**；funasr 上游事件循环阻塞残余风险仍未修，sidecar 有界透传上游状态。仍开放：① relay 稳定性（wslrelay/WSL management 面修复或替代转发路径——监控侧已由 sidecar 旁路绕开，不再阻塞 history/insights）；**语音引擎受控启动与监控全绿复验已闭环（M14-31：2026-09-15 受控 start 双引擎达健康、sidecar 回升 running-healthy、单轮只读监控 34/0/0 全绿、Codex 独立验收 PASS——证据 `docs/evidence/m14-31-voice-engine-recovery/README.md`；单轮全绿不证明长期稳定性）；真实端到端语音链路（ASR/TTS）adapter 冒烟已闭环（M14-33：2026-09-15 一次性真实 adapter 冒烟 PASS 5/5——ASR `local-funasr` 2900ms 非空中文转写、TTS `local-cosyvoice` 27066ms/241964B RIFF/WAV、三进程 PID 与健康前后不变、tracked clean、Codex 独立验收亦 PASS——证据 `docs/evidence/m14-33-real-voice-e2e-smoke/README.md`；单次/单样例/单文本口径）；下一方向：恢复后持续稳定性观察（更长时间窗口的调度连续性与增量语义观察）与真实客户端/LiveKit 会话剩余面、移动端录音播放、长稳（soak）/并发负载、真实客户端负载形态——其中「真实浏览器客户端 + LiveKit」连接第一切片已在受控条件下闭环（M14-35：2026-09-16 R2 修订——当前 LiveKit 以 --node-ip 127.0.0.1 通告媒体且 UDP 仅绑 loopback，Chromium/WebRTC 默认不收集 loopback ICE candidate，默认直连拓扑存在间歇性 ICE 失败：Codex 独立默认拓扑首跑 connect 失败 `could not establish pc connection`、同构建复跑 12/12——拓扑级不确定，非可忽略抖动；验收脚本 R2 起 Chromium 显式加 `--allow-loopback-in-peer-connection` 受控条件（生产用户浏览器默认不具备），gitignored `.verify/m14-35-web-livekit-client-r2/` 受控连跑 run1/run2 通过、run3 connect 步失败、run4/5/6 复跑连续三次通过——末段连续 3/3 为受控拓扑验收通过，非默认浏览器直连稳定性；通过时 token/Room.connect/数据通道/麦克风音轨发布/退出清理五步全 passed（每次检测唯一随机房间，消除固定房间并发互听）、ws_url=ws://127.0.0.1:7880、DOM 无 JWT、检测窗口 console 零错误硬断言；**新生产阻塞项：默认拓扑稳定化需 LAN/TURN/显式绑定（node-ip 与 UDP 绑定面）独立设计改造与验收——已由 M14-37 交付（2026-09-16：验收工具默认/受控模式严格开关 + `AIOS_LIVEKIT_BIND_IP` 媒体面独立绑定 + fail-closed 暴露校验 + 真实默认浏览器 8/8 连跑通过、受控回归 1/1、非法值 fail-closed 探针——证据 `docs/evidence/m14-37-livekit-default-topology/README.md`；剩余：运维显式执行 LAN 绑定切换与切换后默认拓扑复验——**已由 M14-38 于 2026-09-16 执行闭环**（LAN IP `192.168.8.3` 切换 + 最小范围重建 api/livekit + 真实默认浏览器 3/3 + 切换后监控 34 ok/0 warn/0 critical + 九键 pin 防漂移 + cutover/rollback runbook 工具固化——证据 `docs/evidence/m14-38-livekit-lan-cutover/README.md`），远程设备/跨 NAT 仍属 M10-05 TURN 后备域）**；连接诊断切片口径，完整 ASR/TTS 会话/远端订阅/多人/重连/长稳仍开放；证据 `docs/evidence/m14-35-web-livekit-client/README.md`；均既有未决项口径，单次 adapter 冒烟与单次连接切片不覆盖）**。GitHub Actions 外部故障恢复（PR #78/#79/#80/#81/#83 等多轮 0-step 形态失败——**PR #85 起 CI 已恢复真实运行且全绿**，持续观察）、长稳/并发 soak 的剩余面（M14-11 已闭环有界本地彩排：三轮零失败；**未决**：>60s 长稳、真实客户端负载形态、跨机）、真实客户端验收、监控/告警收口的剩余面（M14-12 已交付采集工具并完成首次单次真实只读监控；M14-13 已交付历史索引/有界留存/趋势摘要工具并完成**首次真实历史构建**（canonical 单工件、回填回合独立执行、两遍逐字节相同）；M14-14/M14-21 已交付 monitor→history→insights 三步组合管道 + 计划任务 readiness 管理器 + 静默入口，**M14-22 已验收：真实计划任务三步持续执行（12:30 与 12:45 两轮全 ok、Last Result=0）与 insights 两轮产出/刷新**；**M14-23 已合并并验收：restart 增量语义经 18:00/18:15 两轮自然调度验证（37 连 warn 首轮恢复 ok、增量/事件/恢复计数全 0）**；**未决**：外部告警接入、指标时序存储/查询、阈值随时间的标定、跨机监控、更长时间窗口的调度连续性与增量语义观察（重建/重置/baseline-missing 路径真实触发未发生））、MinIO 镜像停发后的长期替代评估（M14-13 CI R2 已改本地自建 pin 源码镜像；M14-38 记录的「恢复 dry-run 被本机缺自建镜像阻塞」已由 M14-39 于 2026-09-16 收口（健康栈跳过 up + up 前只读预检 fail-closed，真实只读 dry-run exit 0——证据 `docs/evidence/m14-39-recovery-healthy-skip/README.md`），本机自建镜像构建与生产采纳（minio-data 卷属主迁移）已由 M14-40/M14-41 于 2026-09-17 闭环（PR #118 merge `5bc74c4`、真实 execute pass、采纳后 preflight 全过与 recovery dry-run OK），**未决**：Garage/SeaweedFS/Ceph RGW/托管 S3 评估，见 `docs/evidence/m14-13-minio-selfbuild/README.md`）、治理审计链初始锚定缺口已由 M14-42 于 2026-09-17 收口（supervisor 执行 dry-run valid + `--yes` 写入 sequence 0 创世锚 anchor_hash `59c672b9…7c02` + preflight 5/5 只读复核，锚文件与事实入库 `docs/evidence/m14-42-audit-chain-anchor/`；锚文件 WORM 归档工具已由 M14-43 于 2026-09-17 交付（fail-closed 三命令 + S3 对象锁 API 白名单，纯开发验证零真实执行）；真实 WORM 归档执行已由 M14-44 于 2026-09-17 真实闭环（MinIO Object Lock 桶 `aios-audit-worm` COMPLIANCE 归档 + verify pass）；离线介质第二副本已由 M14-49 于 2026-09-18 以**独立物理盘在线副本**形态闭环（G: 独立 Disk 1、preflight/copy/verify 三步 pass——非真正断开/可移动/离站介质，证据 `docs/evidence/m14-49-audit-worm-offline-execution/README.md`）；**未决**：真正离线/离站介质副本与定期审计锚定/归档调度（锚链仅 sequence 0）仍开放；provider 冒烟与发布审批仍未完成）、真机/跨机复现、AGC 签名发布与真机发布链——授权评估前不动工；原列「M14-09 合并后生产 env 补键与首次 Web-only 升级演练」已由 M14-10 完成销项，其余未决项全部保留；全局 `production_ready=false` 不变）
