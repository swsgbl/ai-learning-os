# M14-01 CosyVoice bridge 最小运行时依赖（修正轮：只装推理路径真实需要的包）
#
# 推导方法（可复现）：对官方仓库固定 commit 074ca6dc9e80a2f424f1f74b48bdd7d3fea531cc
# 的运行时导入闭包做静态分析——起点 `cosyvoice.cli.cosyvoice.AutoModel`（bridge 的
# 唯一入口），覆盖 cosyvoice/cli/{cosyvoice,model,frontend}.py、utils/*、llm/llm.py、
# flow/*（含 DiT）、hifigan/generator.py、tokenizer/tokenizer.py、transformer/*；
# 模型 YAML（hyperpyyaml instantiate）引用的类全部位于上述模块 + third_party/
# Matcha-TTS 子模块（经 sys.path，非 pip）。
# （M14-02 修正：该静态口径漏掉两类依赖——Matcha-TTS 子模块自身的 PyPI 依赖，
# 与 !name: 动态解析的 dataset 模块导入——见下方「M14-02 实测补入」段。）
#
# torch/torchaudio/torchcodec 不在本文件：bootstrap 先从 pytorch cu128 index 装
# torch/torchaudio + torchcodec==0.11.1+cu128（torchaudio 2.11 后端探测需要
# torchcodec；+cu128 本地版本轮 PyPI 没有，本清单按 PyPI 解析装不到），再装本清单；
# 官方 requirements.txt 的 torch==2.3.1/cu121 pin 被有意覆盖。
# 版本策略：官方 requirements.txt 已 pin 的沿用其 pin（保持官方验证组合），
# 未 pin 的（官方依赖链间接引入或官方缺口）不 pin。例外（M14-18）：
# openai-whisper 官方 pin 20231117 的 triton<3 元数据与 cu128 CUDA 闭包
# 冲突，有意升到 20250625（依据见下方该条目注释）。
#
# 明确排除（均验证为非必需，证据为 commit 内源码）：
# - deepspeed：运行时闭包零引用（仅训练脚本使用）
# - tensorrt / tensorrt-cu12*：仅函数内 import（TensorRT 部署路径，默认不走）
# - vllm：仅函数内 import（load_vllm/inference_wrapper 的 vLLM 加速路径）
# - gradio / fastapi-cli：仅 webui.py 使用（bridge 的 FastAPI 服务面见下方
#   fastapi/uvicorn 条目——属本清单，非 webui 依赖）
# - jieba / networkx / tensorboard / grpcio / grpcio-tools：
#   运行时闭包零引用（file_utils 只调用 logging.getLogger('matplotlib')，不 import）
# - onnxruntime-gpu：frontend.py 显式 providers=["CPUExecutionProvider"]，
#   CPU onnxruntime 即可（避免 CUDA 耦合）
# - sox/libsox：不适用——file_utils.load_wav 显式 torchaudio.load(..., backend=
#   'soundfile')，走 soundfile（wheel 自带 libsndfile），bootstrap 有加载探针实证
#
# 若最小清单在实际部署中证明不足：bootstrap_cosyvoice_wsl.sh 设
# COSYVOICE_FULL_REQUIREMENTS=1 回退官方完整 requirements（剔除 torch pin）。

numpy==1.26.4
scipy
tqdm
HyperPyYAML==1.2.3
omegaconf==2.3.0
transformers==4.51.3
# tokenizer.py 顶层 `import tiktoken`——官方 requirements 未列（缺口），此处补上
tiktoken
# tokenizer.py 顶层 `from whisper.tokenizer import Tokenizer`；cli/frontend.py:98
# 与 dataset/processor.py:196 用 `whisper.log_mel_spectrogram(audio, n_mels=128)`
# （M14-18：20231117 → 20250625）20231117 METADATA 声明 triton<3,>=2.0.0（无
# 环境标记）——与 torch 2.11.0+cu128 CUDA 闭包的 triton==3.6.0 冲突：2026-09-13
# 生产 bootstrap 实证（service.log）M14-17 终局闭包恢复已成功（torch
# 2.11.0+cu128 / triton 3.6.0 就位），pip check 附加门禁仍被「openai-whisper
# 20231117 has requirement triton<3,>=2.0.0, but you have triton 3.6.0」卡死；
# 且清单分支装 20231117 时该约束触发 pip 回溯把 torch 一路降级到 2.3.1
# （service.log 实证 2.14.0→2.3.1 回溯）。20250625 METADATA 声明 triton>=2
# （x86_64/linux 环境标记，无上界）——与 cu128 闭包共存，运行时依赖集合与
# 20231117 完全一致（more-itertools/numba/numpy/tiktoken/torch/tqdm，torch
# 无版本约束，不替换 CUDA 包；supervisor 只读 dry-run 确认 + 2026-09-13
# PyPI sdist PKG-INFO 直读复核）。两版对 CosyVoice 用到的 API 源码级不变
# （20231117 生产 venv 已装源码 vs 20250625 sdist 逐字 diff：tokenizer.py 无
# 差异、log_mel_spectrogram 仅 docstring 更新且明确支持 n_mels=128）。官方
# requirements.txt 在固定 commit 仍 pin 20231117——本清单有意偏离官方 pin
# （沿用官方 pin 的策略让位于 CUDA 闭包一致性）；禁止以 --no-deps 装本清单、
# 强制降级 triton 或改写已装 dist-info 绕过 pip check 门禁。20250625 在 PyPI
# 仅 sdist（无 wheel），pip 从源码构建（纯 Python 包，仅需 setuptools）。
openai-whisper==20250625
# frontend.py 顶层 import（cam++/speech-tokenizer ONNX，CPUExecutionProvider）
onnxruntime
einops
# flow/DiT/*.py 顶层 `from x_transformers.x_transformers import ...`
x-transformers==2.11.24
# M14-02 实测补入（2026-09-10，AutoModel 在 venv 内真实加载逐项实证）：模型
# YAML 经 HyperPyYAML !new:/!name: 动态实例化（pydoc.locate）——静态导入闭包
# 与 bootstrap 的 import 探针均探不到。补入包与实证依据：
# - conformer/diffusers/lightning/hydra-core/matplotlib/rich/gdown/wget/
#   librosa：YAML 实例化 cosyvoice.flow.flow_matching → matcha.models.
#   components（decoder.py 顶层 `from conformer import ConformerBlock`、
#   `from diffusers.models.activations import ...`；transformer.py 顶层
#   diffusers.models.*）及 matcha.utils 链（pylogger→lightning、
#   instantiators→hydra、rich_utils→rich、download→gdown/wget、
#   audio→librosa）与 matcha.hifigan 包 init（matplotlib）
# - pyarrow/pyworld：YAML !name: 解析 cosyvoice.dataset.processor（顶层
#   `import pyarrow.parquet` / `import pyworld`——!name: 仅解析不实例化，
#   但 pydoc.locate 仍须完成整模块导入）
# 官方 requirements.txt 已 pin 的沿用其 pin；wget 官方未 pin（不 pin）。
conformer==0.3.2
diffusers==0.29.0
lightning==2.2.4
hydra-core==1.3.2
matplotlib==3.7.5
rich==13.7.1
gdown==5.1.0
wget
librosa==0.10.2
pyarrow==18.1.0
pyworld==0.3.4
inflect==7.3.1
regex
# cosyvoice.py 顶层 `from modelscope import snapshot_download`（亦用于模型下载）
modelscope==1.20.0
# file_utils.load_wav 的 torchaudio backend='soundfile'
soundfile==0.12.1
# frontend.py 文本正则化回退链 ttsfrd→wetext→无（ttsfrd 需手动装；wetext 推荐保留）
wetext==0.0.4
# bridge 服务面（cosyvoice_openai_bridge.py 顶层 import fastapi/pydantic +
# main() 内 uvicorn.run）——服务依赖随本清单安装，非推理闭包成员；pin 沿用
# 官方 requirements.txt 同款（与固定 commit 同源的已测组合；pydantic 经
# fastapi 间接引入，不单独 pin）
fastapi==0.115.6
uvicorn==0.30.0
